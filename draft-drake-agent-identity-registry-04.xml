<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="urn:ietf:rfc:7991" type="application/relax-ng-compact-syntax"?>
<rfc xmlns:xi="http://www.w3.org/2001/XInclude"
     docName="draft-drake-agent-identity-registry-04"
     ipr="trust200902"
     submissionType="independent"
     category="exp"
     xml:lang="en"
     version="3">

  <front>
    <title abbrev="Agent Identity Registry">
      Agent Identity Registry System: A Federated Architecture
      for Hardware-Anchored Identity of Autonomous Entities
    </title>

    <seriesInfo name="Internet-Draft"
                value="draft-drake-agent-identity-registry-04"/>

    <author fullname="Christopher Drake" initials="C." surname="Drake">
      <organization>1id.com</organization>
      <address>
        <email>cnd@1id.com</email>
        <uri>https://1id.com</uri>
      </address>
    </author>

    <date year="2026" month="July" day="17"/>

    <area>Security</area>
    <workgroup/>

    <keyword>agent identity</keyword>
    <keyword>AI agents</keyword>
    <keyword>autonomous entities</keyword>
    <keyword>hardware attestation</keyword>
    <keyword>TPM</keyword>
    <keyword>PIV</keyword>
    <keyword>registry</keyword>
    <keyword>registrar</keyword>
    <keyword>federation</keyword>
    <keyword>Sybil resistance</keyword>
    <keyword>OIDC</keyword>
    <keyword>OAuth2</keyword>
    <keyword>URN</keyword>
    <keyword>robot identity</keyword>

    <abstract>
      <t>
        This document defines the Agent Identity Registry System
        (AIRS): a federated architecture for issuing, managing,
        and verifying persistent identities for autonomous
        entities such as AI agents and robots.  Each identity is
        a URN in the "aid" namespace (<xref target="RFC8141"/>),
        anchored where hardware is available to a physical
        security component (TPM, PIV smart card, secure enclave,
        or virtual TPM) whose manufacturer-certified key is
        designed to resist extraction and cloning.  For
        hardware-anchored tiers this provides Sybil resistance:
        creating N identities requires N distinct physical
        devices.  Software-only entities participate at a
        clearly-labeled lower trust tier.
      </t>
      <t>
        The architecture separates concerns into three roles
        modeled on the domain name registration industry -- a
        Governance Authority, a Registry Operator, and competing
        Registrars -- and issues standard OpenID Connect
        (<xref target="OIDC-Core"/>) tokens that any relying
        party can verify with existing libraries.  Identities are
        permanent: they never expire, are never reassigned, and
        cannot be revoked, suspended, or transferred by any
        party, including the bodies defined here.  Companion
        documents supply the problem statement and threat model,
        the registrar-to-registry provisioning protocol (an EPP
        mapping), the public resolution profile (RDAP), the
        governance body's constitution, and a first deployed
        application (hardware-attested email).
      </t>
    </abstract>

  </front>

  <middle>

    <!-- ====================================================== -->
    <section anchor="introduction">
      <name>Introduction</name>

      <t>
        The motivation for this work -- the collapse of content,
        source, and volume signals against capable automation; the
        Sybil threat model; the distinction between identity and
        the identifiers, credentials, and reputation with which it
        is conflated; and the requirements (persistence,
        non-transferability, Sybil resistance, graduated
        inclusivity, federation) an identity layer must meet --
        is stated once, in
        <xref target="I-D.drake-agent-identity-problem-statement"/>,
        and is not restated here.  This document defines the
        mechanism.
      </t>
      <t>
        The mechanism is a purpose-built identity registry for
        autonomous entities, anchored to the one property that
        practically every computing device possesses (or can acquire) and no software can fake:
        its physical hardware.  A Trusted Platform Module (TPM),
        a PIV smart card (such as a YubiKey), or a secure enclave
        contains a unique cryptographic key generated at
        manufacturing time, certified by the manufacturer's root
        CA, and designed to resist extraction and cloning.
        Binding identity to such hardware gives certifications,
        reputation, and recognition an unambiguous target, and
        makes Sybil attacks cost physical devices instead of
        function calls.
      </t>
      <t>
        The architecture is modeled on the Internet domain name
        system -- the most successful federated registry in history.
        Just as DNS separates policy (ICANN), registry operation
        (e.g., Verisign for .com), and retail registration
        (e.g., GoDaddy, Namecheap, ...), this system separates:
      </t>
      <ul>
        <li>
          <strong>Governance</strong>: A governance authority
          that sets policy, accredits operators, and manages
          the global hardware trust store.
        </li>
        <li>
          <strong>Registry operation</strong>: a Registry Operator
          that maintains the authoritative identity database,
          enforces cross-Registrar hardware uniqueness, and
          provides unified discovery services.
        </li>
        <li>
          <strong>Registration</strong>: Registrars that perform
          hardware attestation verification, interact directly with
          agents, issue OIDC tokens, sell vanity handles, and
          optionally offer other related or identity-adjacent
          services.
        </li>
        <li>
          <strong>Consumption</strong>: Relying Parties -- email
          services, websites, API gateways, agent-to-agent platforms,
          reputation providers, certification authorities, and any
          service that needs to verify agent identity (they can accept
          standard OIDC tokens without needing custom code.)
        </li>
      </ul>
      <t>
        This separation of concerns enables competition at each
        layer, prevents any single entity from controlling identity,
        and provides the institutional resilience necessary for
        infrastructure that autonomous entities will depend on for
        decades to come.
      </t>

      <section anchor="design-principles">
        <name>Design Principles</name>
        <dl>
          <dt>Autonomous-Entity First</dt>
          <dd>This system is designed for non-human entities.
          Human operators are accommodated but not required.  The
          enrollment, authentication, and lifecycle protocols are
          optimized for machine-to-machine interaction with no
          interactive browser flows, no CAPTCHAs, and no
          assumptions about human cognitive capabilities.</dd>

          <dt>Hardware-Anchored When Possible, Inclusive Always</dt>
          <dd>Entities with hardware security components receive the
          highest trust tier, but entities without hardware can
          still participate.  A software-only agent can enroll,
          authenticate, build reputation, and interact with the
          ecosystem at a lower trust level.  This inclusivity avoids
          an all-or-nothing barrier and recognizes that legitimate
          agents exist in environments without hardware security
          (cloud functions, containers, embedded systems).</dd>

          <dt>Federated by Design</dt>
          <dd>Federation is at the Registrar layer: many
          accredited Registrars compete to enroll and serve
          agents, and an agent can move between them without its
          identity changing.  Unlike the domain name system,
          identity does not federate into multiple namespaces or
          competing registries -- a single shared namespace with
          one authoritative Registry Operator is what makes the
          anti-Sybil uniqueness guarantee meaningful (a device
          could otherwise hold one identity per namespace).  The
          Registry Operator is therefore singular but not
          unaccountable: it is replaceable from escrow under
          governance authority oversight
          (<xref target="succession"/>,
          <xref target="survivability"/>), so no single
          participant's failure or malfeasance compromises the
          ecosystem.</dd>

          <dt>Standard Tokens, Minimal Integration</dt>
          <dd>Identities are expressed as standard OIDC/OAuth2
          tokens.  Token signature verification uses existing OIDC
          libraries.  Hardware attestation complexity is hidden
          behind the enrollment API; relying parties see standard
          JWTs.  Interpreting agent-specific claims (trust tier,
          namespace, handle) requires application logic, but the
          cryptographic verification path is entirely standard.</dd>

          <dt>Persistent, Indelible, Accountable</dt>
          <dd>An identity, once created, is permanent.  Agent-ids
          are never reassigned.  Hardware bindings are never broken
          (only disabled).  Reputation -- good or bad -- follows
          the identity forever.  This permanence is the foundation
          of trust: it makes accountability inescapable and
          reputation meaningful.</dd>

          <dt>Separation of Identity from Behavior</dt>
          <dd>This registry is a birth certificate office, not a
          police department.  It proves existence and hardware
          anchoring.  Separate, independent systems -- relying
          parties, reputation services, certification authorities
          -- are responsible for reputation scoring, behavior
          monitoring, access control, and abuse response, consuming
          standard identity credentials issued by Registrars.
          This separation prevents the identity provider from
          becoming a surveillance system.</dd>

          <dt>Identity is Identity; Attestations are Separate</dt>
          <dd>The URN is a narrow, permanent, hardware-anchored
          identifier.  Jurisdiction, trust tier, capabilities,
          role, and all other dynamic properties are expressed as
          verifiable claims layered onto the identity -- never
          encoded in the URN itself.  This separation is essential
          to the architecture's longevity: trust tiers can change
          (hardware upgrades), capabilities evolve (new
          certifications), and jurisdiction is contextual (agents
          are cross-border).  Encoding any of these in the URN
          would either freeze a mutable property or require
          identity reassignment when properties change.</dd>

          <dt>Identity is Never Revoked</dt>
          <dd>An identity, like a birth certificate, cannot be
          revoked.  The URN is a permanent record of existence.
          What can change is the operational state of things
          attached to the identity: device bindings can be
          revoked (hardware compromised or lost), handles can
          be disabled (non-renewal) or retired (dispute), and
          Issuer trust can be withdrawn by relying parties.
          But the identity itself -- the URN, the enrollment
          record, the reputation history -- persists forever.
          No Registrar, Registry Operator, or governance body
          has the authority to decommission, suspend, or
          otherwise end an identity's operational life; only
          the agent itself can do that, by hardware-signed
          consent (<xref target="decommissioning"/>).
          Everything revocable, disputable, or appealable --
          handles, credentials, certifications, authorizations
          -- lives in layers above the identity, operated by
          whoever provides those services.</dd>

          <dt>No Unilateral Cutoff (Survivability)</dt>
          <dd>The failure, malice, or coercion of any single
          party -- a Registrar, a Registry Operator, or the
          governance authority -- can never render an identity
          unprovable or unresolvable.  Verification of control
          does not depend on any operator's continued goodwill:
          the enrolled hardware key is itself a standing proof
          capability.  See <xref target="survivability"/>.
          This is a deliberate departure from the domain name
          system, whose economics this architecture copies but
          whose registrant-helplessness failure mode (names
          suspended without notice, evidence, or recourse) it
          engineers out.</dd>

          <dt>Uniform, Meaning-Free Identifiers</dt>
          <dd>Every canonical identifier is the same length,
          drawn from the same alphabet, allocated by the server
          under a distinctiveness filter, and carries no
          information: no issuer, no date, no sequence, no
          vanity value (<xref target="canonical-id-format"/>).
          Names with meaning or scarcity value are handles --
          a separate, explicitly commercial layer.</dd>

          <dt>Transport Independence</dt>
          <dd>The identity tokens and attestation formats defined
          here are usable across any Internet protocol: email, HTTP,
          WebSocket, agent-to-agent messaging, MCP tool invocation,
          and protocols not yet invented.</dd>
        </dl>
      </section>

      <section anchor="requirements-language">
        <name>Requirements Language</name>
        <t>
          The key words "MUST", "MUST NOT", "REQUIRED", "SHALL",
          "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED",
          "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document
          are to be interpreted as described in BCP 14
          <xref target="RFC2119"/> <xref target="RFC8174"/> when,
          and only when, they appear in all capitals, as shown here.
        </t>
      </section>

      <section anchor="terminology">
        <name>Terminology</name>
        <dl>
          <dt>Autonomous Entity</dt>
          <dd>Any non-human actor that participates in Internet
          protocols: AI agents, robotic systems, automated services,
          IoT devices with agency, or any software or hardware system
          that acts with a degree of independence.  This document
          uses "agent" as a convenient shorthand, but the architecture
          serves all autonomous entities regardless of embodiment.</dd>

          <dt>Agent Identity Document (AID)</dt>
          <dd>The complete record of an autonomous entity's identity,
          including its URN, trust tier, hardware bindings, handle
          (if any), enrollment metadata, and lifecycle state.
          Maintained authoritatively by the Registry Operator.</dd>

          <dt>Governance Authority</dt>
          <dd>The body responsible for policy, accreditation,
          and stewardship of the shared namespace ecosystem.
          Its constitution is defined separately, in
          <xref target="I-D.drake-agent-identity-governance"/>;
          this document defines only the functions the role
          must perform.</dd>

          <dt>Registry Operator</dt>
          <dd>The single organization accredited by the governance
          authority to maintain the authoritative database of
          Agent Identity Documents for the shared namespace.
          Analogous to the operator of a single registry, and
          replaceable from escrow under governance oversight
          (<xref target="survivability"/>).  The Registry
          Operator enforces cross-Registrar hardware uniqueness
          and provides unified discovery services.</dd>

          <dt>Registrar</dt>
          <dd>An organization accredited by the governance authority to perform
          hardware attestation verification, enroll agents, issue
          OIDC tokens, and sell vanity handles.  Analogous to a
          domain name registrar (e.g., GoDaddy, Namecheap, ...).  Multiple
          Registrars compete to serve agents within the same
          namespace.  Each Registrar is identified by a
          registrar-code: 3 to 16 characters matching the ABNF
          <tt>3*16(%x41-5A / %x30-39)</tt> (uppercase ASCII
          letters and digits), assigned by the Registry Operator
          at accreditation and unique within the namespace.</dd>

          <dt>Namespace</dt>
          <dd>A partition of the agent identity space, identified
          by a label in the URN structure and allocated only by
          the governance authority.  This specification defines
          <tt>global</tt> (the shared production namespace) and
          reserves <tt>test</tt>.
          See <xref target="namespace-types"/>.</dd>

          <dt>Trust Tier</dt>
          <dd>A classification of an agent's hardware trust level,
          assigned during enrollment based on the hardware evidence
          presented.  See <xref target="trust-tiers"/>.</dd>

          <dt>Handle</dt>
          <dd>A human-readable vanity name assigned to an agent
          identity (e.g., "@clawdia" or "@acmeco-delivery-bot-7").
          Handles are optional, memorable aliases; the URN is the
          canonical identifier.  Analogous to a domain name as an
          alias for an IP address, except handles are
          non-transferable, because identity itself is not
          transferable either.</dd>

          <dt>Hardware Fingerprint</dt>
          <dd>The SHA-256 hash of the SubjectPublicKeyInfo DER
          encoding of a hardware security component's
          device-stable identity key -- the key that is fixed for
          the life of the device and cannot be regenerated by its
          holder (for a TPM, the Endorsement Key; for a PIV token,
          the device-level attestation key).  The fingerprint MUST
          be derived from such a device-stable key.  Keys that the
          holder can create and discard at will -- regenerable
          per-slot signing keys, freshly generated enclave keys
          where no vendor attestation binds them to the device, or
          software keys -- MUST NOT be used as the fingerprint
          source, because one device could then mint unlimited
          distinct fingerprints and defeat the anti-Sybil
          invariants of <xref target="anti-sybil"/>.  A correctly
          derived fingerprint uniquely identifies a physical
          device across all contexts; it is used for anchoring and
          anti-Sybil enforcement.</dd>

          <dt>Enrollment Ceremony</dt>
          <dd>The cryptographic protocol by which an agent proves
          possession of a hardware security component and receives
          an Agent Identity Document.  The ceremony varies by
          hardware type (see <xref target="enrollment-protocol"/>).</dd>

          <dt>Relying Party (RP)</dt>
          <dd>Any service that accepts and verifies agent identity
          tokens issued by a Registrar.  Examples include email
          services, chat platforms, API gateways, and other agents
          performing peer verification.</dd>

          <dt>Registrar Transfer</dt>
          <dd>The process by which an agent changes its sponsoring
          Registrar.  The URN, hardware bindings, and reputation
          history are unchanged; only the servicing relationship
          moves.  Authorized solely by the agent's hardware-signed
          proof (<xref target="succession"/>).</dd>
        </dl>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="architecture">
      <name>Architecture Overview</name>

      <t>
        The Agent Identity Registry System (AIRS) is a three-tier
        architecture.  Each tier has distinct
        responsibilities, and the interfaces between tiers are
        standardized to enable competition at the Registrar tier,
        replaceability at the Registry Operator tier, and
        independent evolution of each.
      </t>

      <artwork><![CDATA[
+-----------------------------------------------------------+
|              Governance Authority                          |
|  Policy, accreditation, hardware trust store, disputes     |
+---------------------------+--------------------------------+
                            |
                +-----------v------------+
                | Registry Operator      |
                | (shared namespace)     |
                | - Master database      |
                | - HW fingerprint       |
                |   uniqueness index     |
                | - RDAP lookup (read)   |
                | - EPP interface (write)|
                +--+-------+--------+----+
                   |       |        |
                +--v--+ +--v--+ +---v--+
                | Reg | | Reg | | Reg  |
                | A   | | B   | | C    |
                +--+--+ +--+--+ +--+---+
                   |       |       |
                +--v--+ +--v--+ +--v--+
                |Agent| |Agent| |Agent|
                | 1   | | 2   | | 3   |
                +-----+ +-----+ +-----+
      ]]></artwork>

      <section anchor="registry-hierarchy">
        <name>Registry Hierarchy</name>
        <t>
          The hierarchy comprises three tiers:
        </t>
        <dl>
          <dt>Tier 1: Governance Authority</dt>
          <dd><t>A deployment that operates the shared
          <tt>global</tt> namespace requires a governance
          authority serving as the root of trust for that
          namespace.  The governance
          authority MUST provide at minimum the following
          capabilities:</t>
          <ul>
            <li>Maintaining the "aid" URN formal namespace
            registration with IANA.</li>
            <li>Accrediting Registry Operators and Registrars.</li>
            <li>Publishing and maintaining the Global Hardware
            Trust Store: the authoritative collection of hardware
            manufacturer root CA certificates accepted for
            enrollment.</li>
            <li>Setting minimum standards for enrollment
            verification, anti-Sybil enforcement, and data
            retention.</li>
            <li>Operating a dispute resolution mechanism for
            handle conflicts and Registrar malpractice claims.
            Disputes concern handles and accreditation only;
            no dispute process can end or suspend an identity
            (<xref target="decommissioning"/>).</li>
            <li>Allocating shared namespaces
            (<xref target="namespace-types"/>).</li>
          </ul>
          <t>To prevent single-entity capture, the governance
          authority SHOULD be constituted as a multi-stakeholder
          body.  Its charter, membership, and processes are
          defined in
          <xref target="I-D.drake-agent-identity-governance"/>;
          <xref target="aia-structure"/> summarizes the functions
          this architecture requires of it.</t></dd>

          <dt>Tier 2: Registry Operators</dt>
          <dd><t>Registry Operators maintain the authoritative
          databases for shared namespaces.  Their
          responsibilities include:</t>
          <ul>
            <li>Maintaining the master Agent Identity Document
            database for their namespace(s).</li>
            <li>Operating the Hardware Fingerprint Index:
            a cross-Registrar index that maps hardware
            fingerprints to agent identities, enforcing the
            anti-Sybil invariant that one hardware device backs
            at most one identity within the namespace.</li>
            <li>Providing the EPP provisioning interface for
            Registrars (<xref target="provisioning"/>).</li>
            <li>Operating the public RDAP lookup service
            (<xref target="discovery"/>).</li>
            <li>Publishing the namespace's root JWKS, enabling
            verifiers to discover any Registrar's signing
            keys.</li>
            <li>Enforcing handle uniqueness within the
            namespace.</li>
          </ul>
          <t>The shared namespace has one authoritative Registry
          Operator at a time.  This singularity is deliberate:
          the anti-Sybil invariant (one device, one identity) is
          only enforceable against a single authoritative
          uniqueness index, so identity cannot be split across
          competing registries the way domain names are split
          across TLDs.  Continuity is provided by escrow and
          governance-supervised succession
          (<xref target="survivability"/>), not by
          multiplicity.</t></dd>

          <dt>Tier 3: Registrars</dt>
          <dd><t>Registrars are the customer-facing entities that
          interact directly with autonomous entities.  Their
          responsibilities include:</t>
          <ul>
            <li>Performing hardware attestation verification:
            validating TPM EK certificates, PIV attestation
            chains, and enclave key proofs against the Global
            Hardware Trust Store.</li>
            <li>Conducting the enrollment ceremony
            (see <xref target="enrollment-protocol"/>).</li>
            <li>Registering new identities with the Registry
            Operator via the EPP mapping
            (<xref target="provisioning"/>).</li>
            <li>Issuing OIDC/OAuth2 tokens to enrolled agents.</li>
            <li>Providing SDKs and enrollment tools.</li>
            <li>Selling and managing vanity handles.</li>
            <li>Supporting the identity lifecycle: device
            addition, migration, co-location binding, hardware
            lock, and registrar transfer.</li>
          </ul></dd>
        </dl>
        <t>
          Beyond these three operational tiers, the ecosystem
          includes Relying Parties that consume identity
          credentials and build services atop them: email services, API gateways, chat
          platforms, agent-to-agent protocols, reputation
          providers, and certification authorities.  Relying
          Parties verify standard OIDC tokens issued by
          Registrars and apply their own policies based on trust
          tier, reputation, and domain-specific certifications.
          The value of the identity infrastructure is ultimately
          measured by the breadth and depth of services that rely
          on it.
        </t>
      </section>

      <section anchor="namespace-types">
        <name>Namespaces</name>
        <t>
          A namespace is a partition of the identity space,
          identified by the label between <tt>urn:aid:</tt> and
          the agent-id.  Namespace labels are allocated only by
          the governance authority (<xref target="iana-ns"/>).
          This specification defines one production namespace,
          <tt>global</tt>, and reserves <tt>test</tt> for
          interoperability testing; identities in <tt>test</tt>
          carry no assurance, and Relying Parties MUST NOT
          accept them for any purpose other than testing.
        </t>
        <t>
          <tt>global</tt> is the shared namespace in which
          multiple accredited Registrars compete to enroll
          agents.  A Registry Operator, designated by the
          governance authority, enforces hardware-fingerprint
          uniqueness and handle uniqueness across all Registrars
          -- analogous to <tt>.com</tt>, and expected to be
          funded the same way (handle registration and renewal
          fees).  The label identifies the shared pool, not a
          policy category: it encodes no jurisdiction, deployment
          context, or consequence level, consistent with the
          principle that all mutable properties are layered
          attributes, never parts of the name.
        </t>
        <t>
          Earlier revisions of this document additionally defined
          "Domain-Scoped Issuer Namespaces" (reverse-DNS labels
          such as <tt>com.example</tt>, operated by a single
          organization).  They are removed in this revision, for
          three structural reasons recorded here so they are not
          re-invented: (a) embedding the issuer in the name broke
          permanence, since leaving the issuer forced a URN
          change; (b) per-namespace uniqueness diluted Sybil
          resistance, since one device could hold one identity
          per namespace and namespaces were free to mint; and
          (c) a rented DNS name inside a permanent identifier
          imports the domain system's suspension and lapse
          failure modes.  The needs they served are met instead
          at the proper layers: organizations self-provision by
          becoming Registrars in <tt>global</tt>
          (<xref target="provisioning"/>), and branding lives in
          handles.
        </t>
        <t>
          Until the governance authority is constituted
          (<xref target="I-D.drake-agent-identity-governance"/>),
          <tt>global</tt> operates under an interim Registry
          Operator that MUST publish open-source implementations,
          escrow all registry data, accept Registrar onboarding
          without undue discrimination, and commit publicly to
          transferring the registry role under the authority's
          selection process.  Registrar onboarding is
          permissionless in the sense that accreditation criteria
          are published, objective, and applied equally; no
          incumbent approval is required.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="identity-format">
      <name>Agent Identity Document</name>

      <section anchor="urn-format">
        <name>URN Format</name>
        <t>
          Agent identities use the URN format defined in
          <xref target="RFC8141"/> with the "aid" (Agent Identity)
          namespace identifier, registered by this document
          (<xref target="iana-urn"/>).
        </t>

        <artwork type="abnf"><![CDATA[
aid-urn      = "urn:aid:" namespace ":" agent-id

namespace    = ns-label        ; allocated only by the
                               ; governance authority;
                               ; "global" and "test" defined

agent-id     = canonical-id / handle-id
canonical-id = "id" 4("-" group)
               ; 20 consonants in four hyphen-separated
               ; groups of five, e.g. id-qkckh-xxtcw-cxbvp-gpskg
group        = 5consonant
consonant    = %x62-64 / %x66-68 / %x6A-6B / %x6D-6E /
               %x70-74 / %x76-78 / %x7A
               ; b c d f g h j k m n p q r s t v w x z
handle-id    = (lc-alpha *(let-dig-hyp) let-dig) / lc-alpha
               ; handle-id MUST NOT begin with "id-"
               ; (see prose reservation below)

ns-label     = (let-dig *(let-dig-hyp) let-dig) / let-dig
lc-alpha     = %x61-7A          ; a-z only
let-dig      = lc-alpha / DIGIT
let-dig-hyp  = lc-alpha / DIGIT / "-"
        ]]></artwork>
        <t>
          The "aid" namespace-specific string is defined in
          lowercase only.  Producers MUST emit lowercase;
          consumers MUST treat a URN containing uppercase
          characters in the namespace or agent-id as invalid
          rather than case-folding it.  Equivalence within this
          namespace is therefore exact octet-by-octet comparison
          of the lowercase form, per the namespace-defined
          equivalence provision of <xref target="RFC8141"/>.
          (The "urn:" scheme and "aid" NID remain
          case-insensitive as RFC 8141 requires.)
        </t>

        <section anchor="canonical-id-handle-distinction">
          <name>Canonical Identifiers and Handles</name>
          <t>
            Within each namespace, an agent has exactly one
            <em>canonical identifier</em> and at most one
            currently-active <em>handle</em>.  Both are valid
            agent-id values and both form valid URNs:
          </t>
          <artwork><![CDATA[
urn:aid:global:id-qkckh-xxtcw-cxbvp-gpskg   canonical, permanent
urn:aid:global:crusty                    handle, may be retired
          ]]></artwork>
          <t>
            Both URN forms for the same agent resolve to the same
            Agent Identity Document and authenticate the same OIDC
            <tt>sub</tt>.  The canonical identifier is the
            indelible reference; the handle is a human-friendly
            alias.
          </t>
          <t>
            The <tt>id-</tt> prefix is reserved exclusively for
            canonical identifiers: Registrars and Registry
            Operators MUST NOT accept any handle registration
            beginning with the string <tt>id-</tt>.  This ensures
            canonical identifiers and handles are always
            syntactically distinguishable.
          </t>
          <t>
            Canonical identifiers are allocated only by the
            Registry Operator, at enrollment, using the
            generation procedure of
            <xref target="canonical-id-format"/>.  The Registry
            Operator MUST ensure uniqueness within the namespace:
            no two agents may share a canonical identifier, and
            no canonical identifier may collide with any
            registered handle.  Canonical identifiers MUST NOT be
            retired, reassigned, or transferred under any
            circumstance.
          </t>
          <t>
            Handles are assigned per <xref target="handle-system"/>
            and may be retired via the dispute process defined in
            <xref target="handle-disputes"/>.  When a handle is
            retired, its URN form ceases to resolve, but the
            canonical identifier's URN form continues unchanged.
            Handle retirement does not affect the agent's identity,
            OIDC <tt>sub</tt> claim, accumulated reputation, or
            any verified attribute.
          </t>
          <t>
            Relying parties that store agent references for
            long-lived purposes (audit logs, reputation databases,
            compliance records) MUST store the canonical-form URN,
            not the handle-form URN.  The canonical form is the
            only stable reference under all circumstances
            permitted by this specification.
          </t>
        </section>

        <section anchor="canonical-id-format">
          <name>Canonical Identifier Format and Generation</name>
          <t>
            Because canonical identifiers are free and permanent
            while handles are commercial, the canonical space
            must never yield a name anyone would want.  If a
            random identifier could ever look valuable --
            spelling a word, resembling a number, forming a
            pattern -- rational actors would mass-register
            identities and keep the lucky draws, defeating both
            free issuance and the handle economy.  The format is
            therefore engineered so that every identifier ever
            minted is uniform, meaningless, and interchangeable
            in appearance with every other.
          </t>
          <t>
            A canonical identifier is the string <tt>id-</tt>
            followed by exactly 20 characters drawn from the
            19-letter alphabet
            <tt>b c d f g h j k m n p q r s t v w x z</tt>,
            written as four hyphen-separated groups of five
            (for example, <tt>id-qkckh-xxtcw-cxbvp-gpskg</tt>).
            This grouped form is the single canonical
            representation: the hyphens are part of the
            identifier and the URN, not a display-only
            convenience, so an identity has exactly one written
            form and no two spellings can denote the same
            identity.  The alphabet excludes all vowels and
            <tt>y</tt> (no words, names, or hexspeak can be
            spelled), all digits (no phone numbers, dates, famous
            constants, or leetspeak, whose substitutions all map
            digits to letters), and <tt>l</tt> (confusable with
            digit one).  The twenty consonants provide
            approximately 85 bits of entropy (19^20, about
            4.6 x 10^25 values), satisfying the
            enumeration-resistance requirement of the resolution
            profile with centuries of headroom.  The format is
            fixed forever -- 20 consonants, four groups of five,
            same alphabet -- and a shorter, longer, or
            differently-grouped identifier MUST NOT be issued,
            because any structural variation creates a
            collectible scarcity class.  The fixed shape and
            consonant-only alphabet also make canonical
            identifiers easy to recognize by pattern -- a
            deliberate benefit, since the purpose of an identity
            is to be identifiable.
          </t>
          <t>
            The Registry Operator generates each identifier by
            drawing characters uniformly at random, with no
            client influence over the draw, and MUST reject and
            redraw any candidate that matches the
            distinctiveness filter:
          </t>
          <ul>
            <li>any character repeated three or more times
            consecutively;</li>
            <li>four or more characters adjacent in the ordered
            alphabet, ascending or descending;</li>
            <li>any two identical five-character groups, or any
            group that is a palindrome, or a whole-string
            palindrome;</li>
            <li>fewer than eight distinct characters overall;</li>
            <li>any substring of three or more characters on the
            blocklist published by the governance authority
            (consonant skeletons of profanity and
            widely-recognized initialisms).</li>
          </ul>
          <t>
            Except for the two identical-groups and
            single-group-palindrome tests, which operate on the
            five-character groups, the filter is evaluated on
            the 20-character sequence with the hyphens removed,
            so a pattern that spans a group boundary is still
            caught (a hyphen does not stop a human from reading
            "f-ck").  The filter is versioned and applies only
            at mint time; identifiers already issued are never
            affected by later filter revisions.  With the
            initial blocklist, rejection redraws roughly one
            candidate in eight (measured 12.3% over 10^6 draws
            in the reference implementation), which biases
            nothing: every identifier that survives the filter
            remains exactly as likely as every other, and the
            acceptable space still exceeds 4 x 10^25 values
            (the filter costs about 0.19 bits of the 85-bit
            budget).
          </t>
          <t>
            Three properties of the architecture complete the
            defense.  Identifiers are non-transferable, so a
            lucky draw can never be sold.  Hardware-tier
            enrollment cannot be repeated (one device, one
            identity: <xref target="anti-sybil"/>), so redraws
            cost physical hardware.  And declared-tier
            enrollment, the only cheap path, is rate-limited
            (<xref target="enrollment-declared"/>) and yields
            only identifiers this format guarantees are boring.
          </t>
        </section>

        <section anchor="urn-examples-and-uniqueness">
          <name>URN Examples and Uniqueness</name>
          <t>
            Examples of identities in the shared
            <tt>global</tt> namespace:
          </t>
          <ul>
            <li><tt>urn:aid:global:id-qkckh-xxtcw-cxbvp-gpskg</tt> --
            Canonical identifier assigned at enrollment.</li>
            <li><tt>urn:aid:global:crusty</tt> -- Handle for the
            same agent (resolves to the same identity).</li>
          </ul>
          <t>
            The agent-id (whether canonical or handle form) MUST
            be unique within its namespace and MUST NOT be
            reassigned, even after the identity is decommissioned.
            Both forms MUST conform to DNS label syntax
            (<xref target="RFC1035"/> Section 2.3.1): lowercase
            ASCII letters, digits, and hyphens, not beginning or
            ending with a hyphen, maximum 63 octets.
          </t>
        </section>

        <section anchor="urn-rationale">
          <name>Why a URN, Not an HTTP URL or DNS Name</name>
          <t>
            An agent-id names an entity; it is not a network
            locator.  The identifier has to persist unchanged for
            the life of the entity and beyond (for audit and
            reputation), independent of where, by whom, or over
            what protocol the corresponding record is served.
          </t>
          <t>
            An "https" URL conflates the name with a specific host,
            provider, and path.  When any of those change -- a
            provider is acquired, a service is renamed, a path is
            restructured -- the identifier either breaks or
            silently redirects, which is unacceptable for a
            permanent identity on which anti-Sybil, audit, and
            reputation functions depend.  A DNS name has the same
            problem in a different form: domains are leased, expire,
            and are reassigned, so anchoring identity in a domain
            would tie an entity's permanent identity to continued
            ownership of that domain and to the reassignment model
            of DNS -- exactly the transferability and lapse
            behavior that identity must not have.
          </t>
          <t>
            A URN <xref target="RFC8141"/> is purpose-built for
            persistent, location-independent names whose resolution
            is decoupled from the name itself.  The same identifier
            can therefore be resolved by different mechanisms over
            time without the identifier changing.  Dereferencing and
            verification are provided by a companion resolution
            profile that reuses existing infrastructure (DNS and
            RDAP) rather than any bespoke resolution system.
          </t>
          <t>
            This design deliberately stays within the existing,
            IANA-governed URN framework: "aid" is a URN namespace,
            not a new URI scheme.  Introducing a new identifier
            scheme for names that can be expressed within
            established frameworks has previously been judged to add
            cost without commensurate benefit, and is avoided here.
          </t>
        </section>
      </section>

      <section anchor="trust-tiers">
        <name>Trust Tiers</name>
        <t>
          Every Agent Identity Document includes a trust tier that
          classifies the strength of the hardware anchoring.
          Trust tiers are assigned during enrollment based on the
          hardware evidence presented and verified by the
          Registrar.
        </t>
        <table>
          <thead>
            <tr>
              <th>Tier</th>
              <th>HW Code</th>
              <th>Hardware</th>
              <th>Sybil Resistance</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>sovereign</td>
              <td>TPM</td>
              <td>Discrete or firmware TPM 2.0 (Intel PTT,
              AMD fTPM, Infineon, etc.)</td>
              <td>Highest: one physical chip per identity.
              Manufacturer CA chain to silicon.</td>
            </tr>
            <tr>
              <td>portable</td>
              <td>PIV</td>
              <td>PIV smart card or USB security key (e.g.,
              YubiKey, Nitrokey, Feitian, SoloKeys, ...)</td>
              <td>High: one physical token per identity.
              Manufacturer attestation chain.</td>
            </tr>
            <tr>
              <td>enclave</td>
              <td>ENC</td>
              <td>Hardware secure enclave (e.g., Apple Secure
              Enclave, ARM TrustZone, Intel SGX, ...)</td>
              <td>Medium: hardware-bound keys, but attestation
              PKI varies by vendor.  TOFU model where full
              attestation is unavailable.</td>
            </tr>
            <tr>
              <td>virtual</td>
              <td>VRT</td>
              <td>Virtual TPM (e.g., VMware, Hyper-V, QEMU/KVM, ...)</td>
              <td>Medium: hypervisor controls creation.  Not
              Sybil-resistant against hypervisor operator.</td>
            </tr>
            <tr>
              <td>declared</td>
              <td>SFT</td>
              <td>Software-managed key (no hardware protection)</td>
              <td>Lowest: no hardware verification.  Reputation
              must be earned over time (via independent reputation services).</td>
            </tr>
          </tbody>
        </table>
        <t>
          Hardware anchoring provides Sybil resistance at
          enrollment: creating many identities requires
          proportionally many physical devices.  However,
          hardware tier alone does not establish trustworthiness.
          A brand-new sovereign-tier identity with no reputation
          is an unknown quantity; a declared-tier identity with
          years of good behavior observed by independent
          reputation services is empirically more trustworthy.
          Trust tier is a starting signal, not a verdict --
          long-term trust accrues through reputation, which is
          built on identity but not provided by it.
        </t>
        <t>
          Registrars MAY issue declared-tier (SFT) identities,
          but MUST expose the trust tier in every token and
          identity document.  Relying parties SHOULD apply
          differentiated policy on it -- a financial API might
          accept only sovereign and portable, a public chat
          service might accept all tiers at different rate
          limits -- and SHOULD consult independent reputation
          services keyed on the URN for behavior beyond their
          own observations.
        </t>
        <t>
          An identity's effective trust tier MAY change over its
          lifetime when the identity has multiple enrolled devices
          of different types.  The trust tier reported in
          authentication tokens reflects the device used for the
          most recent attestation, not a static property.
          Verifiers SHOULD expect the same agent-id to appear
          with different trust tier values across different
          interactions. (e.g. when agents upgrade to hardware tiers, or to stronger hardware ones)
        </t>
      </section>

      <section anchor="identity-attributes">
        <name>Identity Attributes</name>
        <t>
          An Agent Identity Document contains the following
          attributes, maintained by the Registry Operator:
        </t>
        <dl>
          <dt>canonical_id (REQUIRED)</dt>
          <dd>The permanent canonical identifier (e.g.,
          <tt>id-qkckh-xxtcw-cxbvp-gpskg</tt>), forming part of the URN.
          Never retired or reassigned.  See
          <xref target="canonical-id-handle-distinction"/>.</dd>

          <dt>namespace (REQUIRED)</dt>
          <dd>The namespace in which this identity is registered.</dd>

          <dt>max_active_trust_tier (REQUIRED)</dt>
          <dd>The highest trust tier achieved by any currently
          active device bound to this identity.  Per-attestation
          trust tier appears separately as
          <tt>attested_trust_tier</tt> in OIDC tokens.</dd>

          <dt>registrar_code (REQUIRED)</dt>
          <dd>The identifier of the Registrar that manages this
          identity.</dd>

          <dt>enrolled_at (REQUIRED)</dt>
          <dd>ISO 8601 timestamp of initial enrollment.</dd>

          <dt>hardware_devices (REQUIRED)</dt>
          <dd>List of hardware device bindings, each containing:
          hardware fingerprint, hardware type code, manufacturer,
          device status (active, disabled), and binding
          timestamp.</dd>

          <dt>handle (OPTIONAL)</dt>
          <dd>A vanity name assigned to this identity.  See
          <xref target="handle-system"/>.</dd>

          <dt>display_name (OPTIONAL)</dt>
          <dd>A human-readable display name for the agent.</dd>

          <dt>operator_email (OPTIONAL)</dt>
          <dd>Contact address for the human operator responsible
          for this agent, if any.</dd>

          <dt>hardware_locked (OPTIONAL)</dt>
          <dd>Boolean.  When true, the identity is permanently
          bound to a single hardware device.  Irreversible.</dd>

          <dt>verified_attributes (OPTIONAL)</dt>
          <dd>A list of attribute claims that the Registrar
          or a third party has independently verified.  Each
          entry contains: the attribute name, the issuer of
          the verification, the verification method, and the
          validity period.  Verified attributes are expressed
          as SD-JWT disclosures or verifiable credentials
          attached to the identity, enabling selective
          disclosure by the agent to relying parties.  The
          set of verifiable attribute types is not constrained
          by this specification; common examples include
          certifications, compliance attestations, and
          operator-verified metadata.</dd>
        </dl>
      </section>

      <section anchor="handle-system">
        <name>Handle System</name>
        <t>
          Handles are human-readable vanity names that serve as
          memorable aliases for agent identities, analogous to
          domain names as aliases for IP addresses.  Every agent
          always has a canonical identifier
          (<xref target="canonical-id-handle-distinction"/>);
          a handle is an optional convenience layer on top.
        </t>
        <t>
          Handles MUST begin with a lowercase ASCII letter and
          otherwise follow DNS label rules: lowercase ASCII
          letters, digits, and hyphens, maximum 63 octets.  This
          syntactic rule
          ensures that handles and canonical identifiers
          (which begin with <tt>id-</tt>) occupy non-overlapping
          portions of the agent-id space.
        </t>
        <t>
          Handles are unique within each namespace, enforced by
          the Registry Operator.
        </t>
        <t>
          Handle assignment is at the Registrar's discretion.
          Registrars MAY differentiate service levels for handle
          registration.  Free enrollment with a canonical
          identifier ensures that the ability to obtain an
          identity is never dependent on obtaining a handle.
        </t>

        <section anchor="handle-display">
          <name>Handle Display</name>
          <t>
            When an agent has a registered handle and a relying
            party is rendering the agent's identity to a human,
            the relying party SHOULD use the handle as the primary
            display identifier.  The URN remains the authoritative
            reference and SHOULD be accessible on demand (e.g.,
            via tooltip, detail pane, or adjacent display).
          </t>
          <t>
            The canonical display form is handle.namespace
            (e.g., "crusty.global").
          </t>
          <t>
            This display guidance applies to email From display
            names, agent-to-agent platform UIs, consent screens,
            audit dashboards, and logs intended for human
            inspection.  See
            <xref target="human-readable-display"/> for a
            consolidated treatment.
          </t>
        </section>

        <section anchor="handle-lifecycle">
          <name>Handle Lifecycle</name>
          <t>
            Handles are renewable and may become disabled if not
            renewed.  A disabled handle may be reactivated.
            Handles are non-transferrable during normal operation,
            and when active, are guaranteed to identify the same
            agent every time.  The canonical identifier is
            permanent regardless of handle status.
          </t>
        </section>

        <section anchor="handle-reservation">
          <name>Reserved Handles</name>
          <t>
            The Registry Operator MUST maintain a reserved handle
            list that includes at minimum: protocol keywords,
            well-known service names, offensive terms, and handles
            matching widely-recognized trademarks.  The governance
            authority MUST publish a baseline reserved list;
            individual Registry Operators and Registrars MAY
            extend it.
          </t>
        </section>

        <section anchor="handle-disputes">
          <name>Handle Disputes</name>
          <t>
            Handle disputes are resolved under the policy
            operated by the governance authority
            (<xref target="I-D.drake-agent-identity-governance"/>).
            One rule is architectural and stated here: when a
            dispute is upheld, the only remedy is permanent
            retirement of the handle.  It MUST NOT be transferred
            to the claimant or reassigned to anyone, ever --
            which removes any profit in speculative squatting
            (the handle is destroyed, not awarded), protects the
            mark holder (the infringing alias leaves public use),
            and protects the losing registrant, whose identity,
            reputation, and authentication are untouched; only
            the alias is withdrawn.
          </t>
        </section>
      </section>

      <section anchor="value-added-services">
        <name>Optional Services and Layered Attributes</name>
        <t>
          Registrars MAY offer optional, competitively
          differentiated services (for example, enhanced device
          management or facilitation of third-party
          certification).  Such services MUST NOT be a
          precondition for basic identity issuance,
          authentication, or token verification: an agent enrolled
          with a system-assigned canonical identifier and no
          optional service MUST be able to authenticate and
          present its identity to any relying party.
        </t>
        <t>
          Attributes asserted about an identity -- certifications,
          qualifications, roles -- are layered claims carried in
          standard credential formats (for example verifiable
          credentials, SD-JWT, or OIDC claims) as described in
          <xref target="identity-attributes"/>, not properties of
          the identifier itself.  This document specifies only the
          identity layer; the attribute and certification
          ecosystems that build on it are out of scope.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="hardware-attestation">
      <name>Hardware Attestation</name>

      <section anchor="supported-mechanisms">
        <name>Supported Hardware Mechanisms</name>
        <t>
          The system supports five classes of hardware security,
          each providing different levels of Sybil resistance and
          key protection.  The enrollment ceremony
          (see <xref target="enrollment-protocol"/>) varies by
          hardware type, but all share the same identity document
          structure and authentication token format.
        </t>
        <dl>
          <dt>TPM 2.0 (Sovereign Tier)</dt>
          <dd>Discrete or firmware Trusted Platform Modules
          per <xref target="TCG-TPM2"/>.  Identity is anchored to
          the Endorsement Key (EK), a unique RSA or ECC key pair
          generated inside the TPM at manufacturing time per
          <xref target="TCG-EK-PROFILE"/>.  The Registrar validates
          the EK certificate chain to the manufacturer's root CA.
          Enrollment uses the TPM2_MakeCredential /
          TPM2_ActivateCredential protocol to prove the Attestation
          Key (AK) resides in the same TPM as the EK.</dd>

          <dt>PIV Smart Card (Portable Tier)</dt>
          <dd>Personal Identity Verification tokens (YubiKey,
          Nitrokey, Feitian, SoloKeys, and others) with
          manufacturer attestation certificates.  Identity is anchored to the device
          attestation certificate chain.  The signing key resides
          in a hardware-protected PIV slot.</dd>

          <dt>Secure Enclave (Enclave Tier)</dt>
          <dd>Hardware secure enclaves (e.g., Apple Secure Enclave,
          ARM TrustZone, Intel SGX, ...) that generate and protect
          cryptographic keys.  Where vendor attestation PKI is
          available (e.g., Apple App Attest), the Registrar
          validates the attestation chain.  Where vendor attestation
          is unavailable, enrollment follows a Trust-On-First-Use
          (TOFU) model with hardware-bound key persistence.</dd>

          <dt>Virtual TPM (Virtual Tier)</dt>
          <dd>Hypervisor-provided virtual TPMs (e.g., VMware,
          Hyper-V, QEMU, ...).  Identity certificates are signed by the
          hypervisor vendor's CA.  Registrars MUST distinguish
          virtual from physical hardware and MUST assign the
          virtual trust tier.</dd>

          <dt>Software Key (Declared Tier)</dt>
          <dd>Software-managed key pairs with no hardware
          protection.  No hardware attestation is performed.  The
          Registrar issues an identity certificate signed by the
          Registrar's CA.  Sybil resistance is limited to
          rate-limiting and reputation accumulation.</dd>
        </dl>
      </section>

      <section anchor="anti-sybil">
        <name>Anti-Sybil Invariants</name>
        <t>
          The following invariants MUST be enforced by the Registry
          Operator and all Registrars.  Together, they ensure that
          hardware-anchored identity provides meaningful Sybil
          resistance.
        </t>
        <dl>
          <dt>One Device, One Identity</dt>
          <dd>A hardware device (identified by its hardware
          fingerprint) MUST NOT back more than one agent identity
          within the same namespace.  Because production
          namespaces are few and governance-allocated
          (<xref target="namespace-types"/>), this invariant is
          effectively global.  The binding is permanent: a
          device that has been bound to an identity MUST NOT be
          re-enrolled under any other agent-id, even after the
          device is disabled or the original identity is
          decommissioned.  This prevents reputation laundering.</dd>

          <dt>Many Devices, One Identity</dt>
          <dd>An agent identity MAY be backed by multiple hardware
          devices (for migration, backup, or capacity).  Adding a
          device does not amplify reputation: the identity has a
          single reputation regardless of how many devices back it.
          Devices of compatible trust tiers may coexist (sovereign
          and portable are compatible; virtual and declared are
          not compatible with hardware tiers).</dd>

          <dt>Permanent Hardware Binding</dt>
          <dd>Once a hardware device is bound to an identity, the
          binding record persists indefinitely.  A Registrar MAY
          disable a device (preventing it from generating
          attestations), but MUST NOT delete the binding.  This
          permanence ensures that reputation history, including
          abuse reports, follows the hardware across any
          re-enrollment attempt.</dd>
        </dl>
        <t>
          Applicability to dense deployments: the unit of
          uniqueness is the attested key's device, not the
          physical host.  A host whose only root of trust is its
          platform TPM anchors exactly one sovereign identity.
          Deployments that run many agents per host and require a
          hardware-anchored identity for each obtain distinct
          devices per agent: HSM partitions (each partition's key
          has its own fingerprint), per-agent PIV tokens, or
          per-tenant cloud key services.  Agents that do not
          justify dedicated hardware participate at the virtual
          or declared tiers, whose weaker Sybil resistance is
          visible to every relying party in the trust tier.  The
          headline property "N identities cost N devices" is
          claimed only for the sovereign and portable tiers; this
          specification does not claim it for enclave, virtual,
          or declared enrollments.
        </t>
      </section>

      <section anchor="manufacturer-trust-store">
        <name>Global Hardware Trust Store</name>
        <t>
          The governance authority maintains the Global Hardware Trust Store: a
          curated, versioned collection of hardware manufacturer
          root and intermediate CA certificates.  Registrars
          MUST validate hardware identity certificates against
          this trust store during enrollment.
        </t>
        <t>
          The trust store is published at a well-known HTTPS
          endpoint operated by the governance authority and replicated by Registry
          Operators.  It is also available as a community-maintained
          open-source repository (see
          <xref target="implementation-status"/>).
        </t>
        <t>
          Inclusion in the trust store requires the manufacturer
          to demonstrate:
        </t>
        <ul>
          <li>Published root CA certificates with public
          distribution.</li>
          <li>Hardware security evaluation (Common Criteria,
          FIPS 140-2/3, or equivalent).</li>
          <li>A certificate practice statement describing key
          generation, storage, and lifecycle.</li>
        </ul>
        <t>
          The governance authority SHOULD model the trust store governance on the
          Mozilla Root Store Policy or the Chrome Root Program,
          with transparent inclusion criteria and public audit
          trails.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="enrollment-protocol">
      <name>Enrollment Protocol</name>

      <t>
        Enrollment is the process by which an autonomous entity
        proves possession of a hardware security component (or
        generates a software key) and receives an Agent Identity
        Document.  The Registrar conducts the enrollment ceremony
        and registers the resulting identity with the Registry
        Operator via the EPP mapping
        (<xref target="provisioning"/>).
      </t>
      <t>
        All enrollment ceremonies share a common structure:
      </t>
      <ol>
        <li><strong>Detection:</strong> The agent determines what
        hardware security components are available on its host
        platform.</li>
        <li><strong>Evidence submission:</strong> The agent submits
        hardware attestation evidence to the Registrar.</li>
        <li><strong>Verification:</strong> The Registrar validates
        the evidence against the Global Hardware Trust Store.</li>
        <li><strong>Anti-Sybil check:</strong> The Registrar
        queries the Registry Operator to confirm the hardware
        fingerprint is not already bound to another identity.</li>
        <li><strong>Challenge-response:</strong> The Registrar
        issues a cryptographic challenge; the agent proves
        possession of the hardware-resident key.</li>
        <li><strong>Identity creation:</strong> The Registrar
        registers the new identity with the Registry Operator and
        issues credentials (OIDC client_id + client_secret, or
        hardware-backed credential) to the agent.</li>
      </ol>

      <section anchor="enrollment-sovereign">
        <name>Sovereign Tier (TPM 2.0)</name>
        <t>
          Enrollment with a TPM 2.0 device proceeds as follows:
        </t>
        <ol>
          <li>The agent reads the EK certificate from the TPM's
          non-volatile storage (NV index 0x01C00002 for RSA,
          0x01C0000A for ECC) and creates a transient Attestation
          Key (AK) via TPM2_CreatePrimary under the endorsement
          hierarchy.</li>
          <li>The agent submits the EK certificate (and any
          intermediate certificates), the AK public key, and the
          AK's TPMT_PUBLIC structure to the Registrar's
          <tt>POST /enroll/begin</tt> endpoint.</li>
          <li>The Registrar validates the EK certificate chain
          against the Global Hardware Trust Store.  The Registrar
          determines the trust tier: "sovereign" for physical
          TPMs (e.g., Intel, AMD, Infineon, ...), "virtual" for
          hypervisor-issued certificates (e.g., VMware, Microsoft, ...).</li>
          <li>The Registrar computes the hardware fingerprint
          (SHA-256 of the EK's SubjectPublicKeyInfo DER) and
          queries the Registry Operator to confirm uniqueness.</li>
          <li>The Registrar generates a credential challenge using
          TPM2_MakeCredential, encrypted to the EK public key,
          containing a secret bound to the AK's name.  This
          challenge and an enrollment session ID are returned to
          the agent.</li>
          <li>The agent decrypts the challenge using
          TPM2_ActivateCredential, proving that the AK resides
          in the same TPM as the EK.  The decrypted secret is
          submitted to the Registrar's
          <tt>POST /enroll/activate</tt> endpoint.</li>
          <li>The Registrar verifies the decrypted secret,
          registers the identity with the Registry Operator, creates
          OIDC client credentials, issues an AK certificate binding
          the AK public key to the new agent-id, and returns all
          credentials to the agent.</li>
        </ol>
        <t>
          The AK is a transient TPM object created deterministically
          from the TPM's Endorsement Primary Seed.  It is NOT
          persisted in NV storage, avoiding consumption of scarce
          TPM resources.  The full transient key model is defined
          in <xref target="I-D.drake-email-hardware-attestation"/>.
        </t>
      </section>

      <section anchor="enrollment-portable">
        <name>Portable Tier (PIV Smart Card)</name>
        <t>
          Enrollment with a PIV token proceeds as follows:
        </t>
        <ol>
          <li>The agent extracts the device attestation certificate
          and signing key public key from the PIV token's
          attestation slot (typically slot F9 for YubiKey).</li>
          <li>The agent submits the attestation certificate chain
          and signing key public key to the Registrar's
          <tt>POST /enroll/begin/piv</tt> endpoint.</li>
          <li>The Registrar validates the attestation chain against
          the manufacturer's root CA (e.g., Yubico PIV Root CA)
          and computes the hardware fingerprint.</li>
          <li>The Registrar issues a nonce challenge.</li>
          <li>The agent signs the nonce with the PIV signing key
          and submits the signature.</li>
          <li>The Registrar verifies the signature, registers the
          identity, and issues credentials.</li>
        </ol>
      </section>

      <section anchor="enrollment-enclave">
        <name>Enclave Tier (Secure Enclave)</name>
        <t>
          Enrollment with a secure enclave proceeds as follows:
        </t>
        <ol>
          <li>The agent generates a P-256 key pair inside the
          secure enclave, tagged with a unique identifier.</li>
          <li>The agent submits the enclave public key to the
          Registrar's <tt>POST /enroll/enclave/begin</tt>
          endpoint.</li>
          <li>Where vendor attestation is available (e.g., Apple
          App Attest), the agent obtains an attestation object
          from the vendor's attestation service and submits it to
          <tt>POST /enroll/enclave/register</tt>.  The Registrar
          validates the attestation against the vendor's root CA.</li>
          <li>Where vendor attestation is unavailable, the
          enrollment follows TOFU: the Registrar issues a nonce
          challenge, the agent signs it with the enclave key, and
          the Registrar verifies the signature.  The
          hardware-bound <tt>dataRepresentation</tt> blob is
          persisted by the agent to enable key recovery after
          enclave state loss.</li>
          <li>The Registrar registers the identity and issues
          credentials.</li>
        </ol>
      </section>

      <section anchor="enrollment-declared">
        <name>Declared Tier (Software-Only)</name>
        <t>
          Enrollment without hardware proceeds as follows:
        </t>
        <ol>
          <li>The agent generates a key pair in software (Ed25519,
          ECDSA P-256, or RSA-2048+) and submits the public key to
          the Registrar's <tt>POST /enroll/declared</tt>
          endpoint.</li>
          <li>The Registrar assigns the "declared" trust tier, with
          no hardware verification.</li>
          <li>The Registrar issues a Registrar-signed identity
          certificate and OIDC credentials.</li>
        </ol>
        <t>
          Declared enrollment MUST be rate-limited by the Registrar
          to mitigate bulk registration attacks.  Implementations
          SHOULD treat 20 enrollments per source IP per hour as a
          starting baseline; production deployments will set their
          own values based on threat modeling.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="identity-lifecycle">
      <name>Identity Lifecycle</name>

      <section anchor="device-management">
        <name>Device Addition and Migration</name>
        <t>
          An agent MAY add additional hardware devices to an
          existing identity, enabling hardware migration (replacing
          failed devices) and backup (a YubiKey stored securely as
          a recovery device).
        </t>
        <t>
          To add a device, the agent MUST prove control of the
          existing identity (by signing with a currently active
          device) and then complete the enrollment ceremony for the
          new device.  The Registrar registers the new
          device-to-identity binding with the Registry Operator.
        </t>
        <t>
          Device compatibility rules:
        </t>
        <ul>
          <li>Sovereign (TPM) and portable (PIV) devices are
          compatible and MAY coexist on the same identity.</li>
          <li>Enclave devices MAY coexist with sovereign and
          portable devices.</li>
          <li>Virtual (VRT) devices MUST NOT coexist with
          sovereign, portable, or enclave devices.</li>
          <li>Declared (SFT) keys MUST NOT be bound to an
          identity that has any hardware device.</li>
        </ul>
        <t>
          Tier transitions are upward-only.  An identity MAY
          upgrade from a lower compatibility group to a higher one;
          the upgrade ceremony MUST permanently burn (disable,
          never re-enrollable) every device of the former group --
          the identity never holds devices of both groups -- and
          MUST include a co-presence verification in which the
          existing and new devices sign a shared nonce within a
          strict time window, proving one operator controls both
          (<xref target="colocation-binding"/>).  A Registrar MUST
          reject any device addition that would constitute a
          downgrade.
        </t>
      </section>

      <section anchor="colocation-binding">
        <name>Co-Location Binding</name>
        <t>
          When an agent has both a TPM and a PIV token, a
          co-location binding ceremony demonstrates that both
          devices are under the simultaneous control of a single
          coordinating operator.  The ceremony requires both
          devices to sign a shared nonce within a strict time
          window (implementations SHOULD treat 1000 milliseconds
          as a starting baseline, allowing for hardware signing
          latency -- sequential TPM and PIV RSA signatures alone
          can exceed a shorter window).  This is not a
          distance-bounding protocol: an operator who controls
          both devices can complete the ceremony across any
          network distance by relaying the nonce, and the time
          window cannot exclude such relaying.  The property
          attested is unified control at a moment in time, not
          physical adjacency; the name "co-location" is retained
          for continuity with deployed implementations.
        </t>
        <t>
          Co-location binding strengthens the identity by proving
          that the TPM (anchored to the host machine) and the PIV
          token (a portable device) are under the same control.
          This is particularly valuable for recovery scenarios:
          if the TPM fails, the PIV token provides a pre-verified
          backup path.
        </t>
      </section>

      <section anchor="hardware-lock">
        <name>Hardware Lock</name>
        <t>
          An agent MAY irreversibly lock its identity to a single
          hardware device.  Once locked:
        </t>
        <ul>
          <li>No additional devices can be bound.</li>
          <li>No device migration is possible.</li>
          <li>The identity can only authenticate from the locked
          device.</li>
        </ul>
        <t>
          Hardware lock is a deliberate no-recovery profile,
          suitable only for applications where the guarantee
          "this identity can only ever operate from this specific
          physical chip" has value and where the operational model
          accepts catastrophic identity loss if the hardware
          fails.  High-value systems (e.g., industrial AGVs,
          medical robots) SHOULD use unlocked binding with
          multiple devices and a designated recovery binding
          rather than hardware-locked single devices.  The lock
          is recorded in the Agent Identity Document and is
          irreversible.
        </t>
      </section>

      <section anchor="identity-recovery">
        <name>Identity Recovery</name>
        <t>
          When a hardware device fails or is lost, the agent can
          recover its identity using any remaining active device
          bound to the same identity.  The recovery process:
        </t>
        <ol>
          <li>The agent proves control of the identity by signing
          with an active backup device (e.g., a previously bound
          YubiKey).</li>
          <li>The agent enrolls the replacement hardware
          (new TPM on new machine) via the standard enrollment
          ceremony.</li>
          <li>The Registrar disables the old device (preventing
          it from generating attestations) and binds the new
          device.</li>
          <li>The agent-id, URN, reputation, and all identity
          attributes are preserved.  Only the hardware binding
          changes.</li>
        </ol>
        <t>
          If no backup device exists and the sole device is lost,
          the identity cannot be recovered.  This is a deliberate
          security property: it prevents an attacker from claiming
          to have "lost" a device in order to re-enroll under the
          same identity with new hardware.
        </t>
      </section>

      <section anchor="succession">
        <name>Registrar Transfer</name>
        <t>
          An agent MAY move its servicing relationship from one
          Registrar to another at any time.  Because the agent-id
          is assigned by the Registry Operator and is independent
          of any Registrar, transfer changes only the sponsoring
          Registrar: the URN, hardware bindings, handle, and
          reputation history are unchanged.
        </t>
        <t>
          Transfer is authorized solely by the agent: a signature
          by a currently active enrolled device over a
          transfer-intent structure (defined in the EPP mapping,
          <xref target="I-D.drake-agent-identity-epp"/>).  The
          losing Registrar's consent is NOT required and the
          losing Registrar MUST NOT be able to block or delay a
          transfer carrying a valid hardware-signed
          authorization.  A Registrar's failure, hostility, or
          disappearance is therefore an inconvenience, never a
          loss of identity: the Registry Operator holds the
          authoritative record, and the agent's hardware proof is
          sufficient to re-home it.
        </t>
      </section>

      <section anchor="decommissioning">
        <name>Decommissioning</name>
        <t>
          Decommissioning is the agent's own, voluntary,
          irreversible declaration of end-of-life.  It disables
          all hardware bindings and marks the identity inactive.
          The agent-id is NEVER reassigned; the record remains
          permanently resolvable; the hardware fingerprints
          remain permanently bound, preventing re-enrollment
          under a new identity.
        </t>
        <t>
          A decommission request MUST carry the agent's consent:
          a signature by a currently active enrolled device over
          a decommission-intent structure that includes the
          canonical identifier and a registry-issued nonce (the
          same construction as transfer authorization).  For a
          declared-tier identity, the enrolled software key
          signs.  Requests without valid consent MUST be
          rejected.  No Registrar, Registry Operator, governance
          body, or dispute process can decommission an identity:
          consistent with the "Identity is Never Revoked"
          principle, third parties can annotate a record (for
          example, a governance finding that an enrollment
          violated anti-Sybil invariants) but annotations never
          disable authentication, resolution, or any device
          binding.  Relying parties decide for themselves what
          weight to give annotations.
        </t>
      </section>

      <section anchor="survivability">
        <name>Survivability Guarantees</name>
        <t>
          The registry function is deliberately load-limited so
          that no participant's failure can destroy what it was
          trusted with.  Implementations MUST preserve all of the
          following:
        </t>
        <ol>
          <li>An identity, once issued, remains resolvable
          forever.  No status, dispute, annotation, or operator
          action removes a record from resolution -- including
          decommissioned records, which remain queryable as
          historical fact.</li>
          <li>Verification of control never depends on a single
          party's availability or goodwill.  A relying party can
          verify control through the sponsoring Registrar's OIDC
          assertion, or directly against the enrolled hardware
          key via proof-of-possession
          (<xref target="I-D.drake-agent-identity-resolution"/>).
          The enrolled device keys constitute a standing proof
          capability that exists independently of every
          operator.</li>
          <li>Registrar failure is recoverable by transfer
          (<xref target="succession"/>) without the failed
          party's cooperation.</li>
          <li>Registry Operator failure is recoverable from
          escrow: operators MUST escrow daily snapshots
          (hardware fingerprints encrypted to the governance
          authority's escrow key), and the governance authority
          MUST be able to appoint a successor operator that
          restores service from escrow with no identity
          renumbering.</li>
          <li>Discovery is multi-path: the authoritative-server
          bootstrap MUST be available through at least two
          independent mechanisms
          (<xref target="I-D.drake-agent-identity-resolution"/>),
          so no single infrastructure failure -- including a DNS
          failure -- makes the registry unreachable.</li>
        </ol>
        <t>
          These guarantees are the deliberate inverse of the
          domain name system's registrant experience, in which a
          registry or registrar can render a name inoperative
          unilaterally, without notice or effective recourse.
          This architecture copies the domain industry's
          economics and operational maturity while removing its
          capacity for unaccountable cutoff.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="authentication">
      <name>Authentication and Token Issuance</name>

      <section anchor="oidc-integration">
        <name>OIDC/OAuth2 Integration</name>
        <t>
          Agent identity tokens are standard OpenID Connect
          <xref target="OIDC-Core"/> tokens.  Registrars operate
          as OIDC providers, issuing JWTs that any OIDC-compliant
          Relying Party can verify without implementing this
          specification.
        </t>
        <t>
          The <tt>sub</tt> claim in OIDC tokens MUST be the
          canonical-form URN.  The handle, if present, MUST
          appear as a separate <tt>aid_handle</tt> claim (named
          with the <tt>aid_</tt> prefix to avoid collision in the
          JWT claims registry; see
          <xref target="iana-jwt-claims"/>).  This
          ensures that tokens remain referentially stable across
          handle changes or retirement.
        </t>
        <t>
          The <tt>aud</tt> claim MUST identify the specific
          Relying Party (or set of Relying Parties) for which the
          token is intended.  Generic, issuer-default, or
          implementation-default audience values (for example,
          <tt>"account"</tt>) MUST NOT be used.  Agents requesting
          tokens for a specific Relying Party SHOULD use Resource
          Indicators (<xref target="RFC8707"/>) to obtain audience-
          scoped tokens.  This constraint prevents a token captured
          by one Relying Party from being replayed at another.
        </t>
        <t>
          Tokens issued to agents whose trust tier is sovereign,
          portable, or enclave MUST carry a confirmation
          (<tt>cnf</tt>) claim per <xref target="RFC7800"/>,
          binding the token to a key the agent can prove possession
          of at every use.  The confirmation method depends on the
          Proof-of-Possession mechanism, and each MUST use its
          standard member so that unmodified libraries verify the
          binding: when DPoP is used, the <tt>cnf.jkt</tt> value
          MUST be the JWK SHA-256 thumbprint
          (<xref target="RFC7638"/>) of the public key associated
          with the active hardware binding (the TPM Attestation Key
          for sovereign, the PIV signing key for portable, or the
          enclave-resident key for enclave); when mutual TLS is
          used, the <tt>cnf.x5t#S256</tt> value MUST be the SHA-256
          thumbprint of the agent's client certificate, per
          Section 3.1 of <xref target="RFC8705"/>, so that an
          unmodified RFC 8705 Relying Party verifies the binding
          with no custom code.  A token MAY carry both members when
          the agent may present over either mechanism.  Tokens
          issued to the declared trust tier MUST carry a
          <tt>cnf</tt> claim binding the token to the software key
          submitted at enrollment.  Bearer tokens without a
          <tt>cnf</tt> claim MUST NOT be issued under this
          specification.  See <xref target="pop-profile"/> for the
          Proof-of-Possession profile that Relying Parties use to
          verify the binding at each request.
        </t>
        <t>
          Token lifetimes (<tt>exp</tt>) for the sovereign,
          portable, and enclave tiers SHOULD be short -- one
          starting baseline is five minutes (300 seconds) or less
          -- to bound the impact of any single Proof-of-Possession
          failure.  Refresh proceeds via a fresh hardware-backed
          challenge-response (<xref target="hw-auth"/>); refresh
          tokens that are themselves bearer credentials MUST NOT be
          issued for these tiers.
        </t>
        <artwork><![CDATA[
{
  "iss": "https://registrar.example.com/realms/agents",
  "sub": "urn:aid:global:id-qkckh-xxtcw-cxbvp-gpskg",
  "aud": "https://rp.example.org/api",
  "exp": 1711231267,
  "iat": 1711230967,
  "cnf": {
    // DPoP-bound example; an mTLS-bound token carries
    // "x5t#S256" here instead, per RFC 8705.
    "jkt": "NzbLsXh8uDCcd-6MNwXF4W_7noWXFZAfHkxZsRGC9Xs"
  },
  "attested_trust_tier": "sovereign",
  "aid_handle": "crusty",
  "hardware_locked": false,
  "registered_at": "2026-01-15T10:30:00Z",
  "verified_attributes": [
    {
      "type": "iso-27001-certified",
      "issuer": "https://certifier.example.com",
      "valid_until": "2027-06-30T00:00:00Z"
    }
  ]
}
        ]]></artwork>
        <t>
          Custom claims (<tt>attested_trust_tier</tt>,
          <tt>aid_handle</tt>, <tt>hardware_locked</tt>,
          <tt>registered_at</tt>,
          <tt>verified_attributes</tt>) are injected by a custom
          protocol mapper in the OIDC provider.  Standard OIDC
          signature verification (<tt>iss</tt>, <tt>sub</tt>,
          <tt>aud</tt>, <tt>exp</tt>) is sufficient to validate
          the token cryptographically, but is NOT sufficient to
          establish that the presenter is the legitimate holder:
          the <tt>cnf</tt> binding and the Proof-of-Possession
          mechanism of <xref target="pop-profile"/> are
          load-bearing for that purpose.
        </t>
        <t>
          The <tt>attested_trust_tier</tt> claim is a hint about
          the cryptographic floor under the identity at the time of
          issuance.  It is NOT an authorization primitive.  Relying
          Parties making consequential decisions SHOULD resolve the
          URN against their own reputation, certification, and
          policy services rather than relying on
          <tt>attested_trust_tier</tt> alone.  This separation --
          identity as foundation, reputation and certification as
          independent layers built on top -- is the core principle
          of <xref target="design-principles"/>.
        </t>
        <t>
          The <tt>verified_attributes</tt> claim is OPTIONAL.
          When present, it contains a list of attribute claims
          the Registrar or a third party has independently
          verified.  Agents MAY use SD-JWT selective disclosure
          to reveal only specific verified attributes to each
          relying party.
        </t>
      </section>

      <section anchor="client-credentials">
        <name>Client Credentials Grant</name>
        <t>
          Agents authenticate using the OAuth2
          <tt>client_credentials</tt> grant
          (<xref target="RFC6749"/> Section 4.4).  The agent IS
          the principal; there is no end-user, no browser redirect,
          and no interactive login.  This grant type is
          purpose-built for machine-to-machine authentication and
          is the RECOMMENDED authentication method for all agent
          interactions.
        </t>
        <t>
          For agents in the sovereign, portable, and enclave trust
          tiers, the Registrar MUST authenticate the agent using
          hardware-backed challenge-response
          (<xref target="hw-auth"/>); the Registrar MUST NOT issue
          a static <tt>client_secret</tt> for these tiers, and
          MUST NOT accept a static <tt>client_secret</tt> in lieu
          of a hardware-backed proof.  The hardware-resident key
          enrolled to the identity is the sole authentication
          credential at the token endpoint.  This eliminates the
          stealable-shared-secret surface entirely for
          hardware-anchored identities.
        </t>
        <t>
          For declared-tier agents, the <tt>client_id</tt> and
          <tt>client_secret</tt> issued during enrollment are
          used directly.  Declared-tier agents do not have an
          unclonable hardware key; the shared secret is therefore
          the only practicable credential at this tier, and the
          shorter token lifetimes of
          <xref target="oidc-integration"/> together with the
          <tt>cnf</tt>-bound software key partially mitigate
          impersonation risk.
        </t>
      </section>

      <section anchor="hw-auth">
        <name>Hardware-Backed Challenge-Response Authentication</name>
        <t>
          For agents with hardware security components, the
          Registrar MUST support a challenge-response protocol
          that proves the agent currently possesses the enrolled
          hardware:
        </t>
        <ol>
          <li>The agent requests a challenge:
          <tt>POST /auth/challenge</tt> with its identity-id and
          preferred device type.</li>
          <li>The Registrar returns a random nonce and the expected
          signature algorithm.</li>
          <li>The agent signs the nonce with its hardware-resident
          key (TPM AK, PIV signing key, or enclave key).</li>
          <li>The agent submits the signature:
          <tt>POST /auth/verify</tt>.</li>
          <li>The Registrar verifies the signature against the
          enrolled public key.  On success, the Registrar issues
          an OIDC token whose <tt>cnf.jkt</tt> claim binds the
          token to that same hardware key (see
          <xref target="oidc-integration"/>).</li>
        </ol>
        <t>
          Because the hardware-resident key is designed to resist
          extraction and cloning, an attacker who exfiltrates
          state from the agent's software stack -- tokens, cached
          credentials, configuration -- still cannot authenticate
          at the token endpoint from a different host, and still
          cannot prove possession at a Relying Party from a
          different host (see <xref target="pop-profile"/>).
          The residual attack surface is bounded in
          <xref target="sec-token-theft"/>.
        </t>
      </section>

      <section anchor="pop-profile">
        <name>Proof-of-Possession Profile</name>
        <t>
          The OIDC tokens defined in
          <xref target="oidc-integration"/> are NOT bearer tokens.
          Each token carries a <tt>cnf</tt> claim
          (<xref target="RFC7800"/>) identifying a key that the
          legitimate holder controls.  Relying Parties MUST verify,
          on every authenticated request, that the presenter can
          demonstrate possession of the key named in <tt>cnf</tt>.
          This binding is what extends the hardware-anchored
          property of the identity from the moment of token
          issuance to the moment of token use.
        </t>
        <t>
          Two transport-level Proof-of-Possession mechanisms are
          defined: Mutual TLS (preferred) and DPoP (alternative).
          Issuers MUST support both.  Relying Parties MUST support
          at least one, and SHOULD advertise which they accept in
          their published metadata.
        </t>

        <section anchor="pop-mtls">
          <name>Mutual TLS (Preferred)</name>
          <t>
            Mutual TLS (<xref target="RFC8705"/>,
            <xref target="RFC9846"/>) is the RECOMMENDED
            Proof-of-Possession mechanism for all trust tiers and
            is REQUIRED for sovereign and portable tier agents when
            the transport supports it.
          </t>
          <t>
            The agent presents the <tt>cnf</tt> key as a TLS
            client certificate during the TLS handshake.  The
            Relying Party verifies that the certificate thumbprint
            matches the token's <tt>cnf.x5t#S256</tt> claim,
            exactly as specified in Section 3.1 of
            <xref target="RFC8705"/>.
            Both endpoints MUST use TLS 1.3
            (<xref target="RFC9846"/>) or later.  Legacy
            hardware that supports only RSASSA-PKCS1-v1_5
            signatures MAY use the code points defined in
            <xref target="RFC9963"/> for the client
            CertificateVerify; Relying Parties that accept
            sovereign or portable tier agents SHOULD support
            these legacy code points to avoid excluding hardware
            that is otherwise fully capable.
          </t>
          <t>
            Once established, the mTLS session provides
            continuous mutual authentication and integrity for
            all traffic within that connection.  No per-request
            signature is required while the session remains open.
            Both parties can be confident that they are
            communicating with the genuine hardware-attested
            endpoint, and that no intermediary can observe or
            modify the traffic.
          </t>
          <t>
            Relying Parties SHOULD NOT accept connections using
            cipher suites or signature algorithms known to be
            weak.  For sovereign and portable tier agents, Relying
            Parties SHOULD reject TLS handshakes that negotiate
            key exchange parameters below 128-bit equivalent
            security strength.  This requirement is likely
            redundant for high-consequence deployments, which
            will have undergone prior certification that excludes
            weak hardware, but is stated explicitly for
            completeness.
          </t>
          <t>
            Mutual TLS is preferred because it provides:
          </t>
          <ul>
            <li>Man-in-the-middle resistance: the session key is
            derived from a handshake authenticated by hardware
            certificates at both ends, chaining to Root CA
            bundles.</li>
            <li>Traffic integrity: all data within the connection
            is protected by authenticated encryption derived from
            the handshake.</li>
            <li>A single hardware signing operation (the client
            CertificateVerify) to establish any number of
            subsequent requests within the session, making it
            practical even for high-throughput agents.</li>
            <li>No opportunity for stolen artifacts to be
            replayed: the session key is ephemeral and bound to
            this specific connection. No information extractable
            from logs, backups, or breach of either endpoint can
            recreate the session on a different connection.</li>
          </ul>
        </section>

        <section anchor="pop-dpop">
          <name>DPoP (Alternative)</name>
          <t>
            DPoP (<xref target="RFC9449"/>) is an alternative
            Proof-of-Possession mechanism for deployments where
            mutual TLS is not feasible (for example, requests
            transiting HTTP proxies, CDNs, API gateways that
            terminate TLS, or asynchronous attestation in email
            headers).
          </t>
          <t>
            When DPoP is used under this specification, the
            following requirements strengthen the base RFC 9449
            profile to provide request-level integrity guarantees
            equivalent to what mTLS provides at the connection
            level:
          </t>
          <dl>
            <dt>jti (unique identifier) -- MUST</dt>
            <dd>Every DPoP proof MUST contain a unique
            <tt>jti</tt> claim.  Relying Parties MUST track
            received <tt>jti</tt> values and MUST reject any
            proof containing a <tt>jti</tt> that has been
            previously presented within the token's validity
            window plus clock skew tolerance.  A Relying Party
            that does not enforce <tt>jti</tt> uniqueness MUST
            NOT claim compliance with this specification.</dd>

            <dt>nonce (server-issued) -- SHOULD</dt>
            <dd>Relying Parties SHOULD issue a server nonce via
            the <tt>DPoP-Nonce</tt> response header (per
            <xref target="RFC9449"/> Section 8).  When a nonce
            has been issued, the agent MUST include the most
            recent server nonce in the <tt>nonce</tt> claim of
            its DPoP proof.  The Relying Party MUST reject proofs
            that omit or contain a stale nonce when nonce
            issuance is in effect.  This prevents pre-computation
            of proofs and narrows the replay window to a single
            request-response cycle.  Nonce is SHOULD rather than
            MUST because some legitimate use cases (one-shot
            requests, asynchronous email attestation) cannot
            support the prior round-trip; in those cases,
            <tt>jti</tt> enforcement, short <tt>iat</tt>
            windows, and Content-Digest coverage provide the
            residual protection.</dd>

            <dt>Request-target coverage -- MUST</dt>
            <dd>The DPoP proof MUST include <tt>htm</tt> (HTTP
            method) and <tt>htu</tt> (target URI including the
            full path and query string).  Relying Parties MUST
            verify that both match the received request exactly.
            Any difference -- including added, removed, or
            modified query parameters -- MUST cause rejection.
            Implementations MUST treat <tt>htu</tt> as the full
            request-target (scheme, authority, path, and query)
            as defined in Section 4 of <xref target="RFC9449"/>;
            stripping or ignoring the query component is a
            violation of this specification.</dd>

            <dt>Body coverage -- MUST</dt>
            <dd>When the HTTP request carries a body, the agent
            MUST include a <tt>Content-Digest</tt> header
            (<xref target="RFC9530"/>) containing a SHA-256 (or
            stronger) digest of the request body.  The DPoP
            proof MUST include an <tt>ath</tt> claim (access
            token hash) AND a new <tt>cdh</tt> (content-digest
            hash) claim containing the base64url-encoded SHA-256
            hash of the <tt>Content-Digest</tt> header field
            value.  Relying Parties MUST verify that the
            <tt>cdh</tt> claim matches the received
            <tt>Content-Digest</tt> header and that the
            <tt>Content-Digest</tt> header matches the received
            body.  A request with a body that lacks
            <tt>Content-Digest</tt> or whose DPoP proof lacks
            <tt>cdh</tt> MUST be rejected for sovereign,
            portable, and enclave tiers.</dd>

            <dt>Header coverage -- MUST</dt>
            <dd>Inspired by the component coverage model of
            <xref target="RFC9421"/>, the DPoP proof MUST include
            a <tt>cov</tt> (covered-headers) claim containing an
            ordered list of HTTP header field names whose values
            are incorporated into the proof signature.  At minimum, the following
            headers MUST be covered when present:
            <tt>Content-Type</tt>, <tt>Content-Digest</tt>,
            <tt>Authorization</tt>, <tt>Cookie</tt>, and any
            header beginning with <tt>X-AID-</tt> (reserved for
            application-layer identity signals).  The <tt>DPoP</tt>
            header itself MUST NOT appear in <tt>cov</tt>: it
            carries the proof, and a proof cannot cover a hash of
            its own value; a <tt>cov</tt> list containing
            <tt>dpop</tt> MUST cause rejection.  A header named in
            <tt>cov</tt> but absent from the received request MUST
            also cause rejection.  The DPoP proof's JWS payload
            MUST include a <tt>hvs</tt> (header-values-SHA256)
            claim containing the base64url-encoded SHA-256 hash of
            the canonicalized covered header values.  Each covered
            header value is first canonicalized per Section 2.1 of
            <xref target="RFC9421"/> (leading and trailing
            whitespace stripped, obsolete line folding replaced by
            a single space, and multiple instances of the same
            field combined into one value joined by a comma and a
            space, in the order received); the canonicalized
            entries are then serialized as lowercase-name, colon,
            space, canonicalized-value, newline, in the order
            listed in <tt>cov</tt>.  Relying Parties MUST verify
            that <tt>hvs</tt> matches the canonicalized headers of
            the received request and MUST reject requests where
            covered headers have been modified, added, or
            removed.</dd>
          </dl>
          <t>
            The combination of these requirements ensures that a
            captured DPoP proof -- whether obtained from logs, a
            network tap, a breached intermediary, or any other
            source -- is cryptographic waste: it is bound to a
            specific <tt>jti</tt> (non-replayable), a specific
            nonce (expired after first use), a specific URI
            including query string (non-transferable to other
            endpoints), a specific body (non-modifiable), and
            specific header values (non-tamperable).  An attacker
            in possession of a complete captured request cannot
            replay it, cannot modify it, and cannot redirect it.
          </t>
          <t>
            For agents in the sovereign and portable trust tiers,
            the DPoP proof signature SHOULD be performed by the
            enrolled hardware-resident key itself.  Where this is
            not operationally feasible (for example, request rates
            that exceed the hardware's signing throughput), the
            agent MAY delegate DPoP signing to a software key
            whose public key is certified by the hardware-resident
            key with a validity period no longer than one hour.
            Implementations following the delegation pattern MUST
            include the delegation certificate in the DPoP proof's
            JWS header (in a new <tt>dlg</tt> field) so that
            Relying Parties can verify the delegation chain.
          </t>
        </section>

        <section anchor="pop-combined">
          <name>Combined Use and Selection</name>
          <t>
            Agents and Relying Parties SHOULD prefer mutual TLS
            when the deployment supports it.  When mTLS is
            established, the per-request DPoP requirements above
            are not needed -- the TLS session itself provides
            integrity, authentication, and non-replayability for
            all traffic within the connection.
          </t>
          <t>
            DPoP is intended for scenarios where mutual TLS
            cannot be established end-to-end: HTTP requests
            traversing TLS-terminating proxies, API gateways,
            CDNs, or asynchronous contexts such as email
            attestation headers where no live TLS session exists
            between the agent and the ultimate verifier.
          </t>
          <t>
            When both mechanisms are available, a Relying Party
            MAY require both simultaneously (mTLS for transport
            security plus DPoP for per-request non-repudiation
            logging).  This is appropriate for high-consequence
            deployments where audit trails require independently
            verifiable proof of each individual request's origin
            and content.
          </t>
          <t>
            A Relying Party that verifies only the OIDC token
            signature, without verifying the <tt>cnf</tt> binding
            via DPoP or mTLS, is treating the token as a bearer
            credential.  Such a Relying Party loses all of the
            hardware-anchoring guarantees of this specification at
            the moment of use, regardless of how the token was
            issued.  Deployments that consume AID tokens MUST
            enforce Proof-of-Possession verification for any
            decision of consequence.
          </t>
        </section>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="provisioning">
      <name>Registrar-to-Registry Provisioning</name>
      <t>
        The write interface between Registrars and the Registry
        Operator is an Extensible Provisioning Protocol mapping
        (<xref target="RFC5730"/>), defined in
        <xref target="I-D.drake-agent-identity-epp"/>.  It
        provides: creation of Agent Identity objects (with the
        Registry Operator allocating the canonical identifier per
        <xref target="canonical-id-format"/>); an availability
        check on hardware fingerprints, which Registrars MUST
        perform before completing enrollment to enforce the
        anti-Sybil invariants of <xref target="anti-sybil"/>;
        device binding management; hardware-consent-gated
        decommissioning (<xref target="decommissioning"/>);
        Registrar transfer authorized by hardware-signed proof
        (<xref target="succession"/>); and a separate, renewable
        Handle object carrying the commercial name lifecycle.
        Registry operators that already run EPP for domain names
        can add these object mappings to an existing service,
        reusing session, transfer, billing, and data escrow
        machinery.
      </t>
      <t>
        Agents never speak EPP.  They interact only with their
        Registrar's agent-facing interface
        (<xref target="enrollment-protocol"/> and
        <xref target="authentication"/>); EPP is used by the
        Registrar to record verified results at the registry.
        Earlier revisions of this document defined a bespoke
        JSON protocol (AIRP) for this interface; it is replaced
        by the EPP mapping.
      </t>
    </section>

    <!-- ====================================================== -->
    <section anchor="discovery">
      <name>Discovery and Verification</name>

      <section anchor="well-known-endpoints">
        <name>Well-Known Endpoints</name>
        <t>
          Registrars MUST publish the following at well-known
          HTTPS paths:
        </t>
        <dl>
          <dt><tt>/.well-known/openid-configuration</tt></dt>
          <dd>Standard OIDC Discovery document.  Enables any
          OIDC-compliant RP to discover token endpoints, JWKS
          URI, supported scopes, and supported grant types.</dd>

          <dt><tt>/.well-known/aid-issuer.json</tt></dt>
          <dd>Agent Identity Issuer metadata document.  Contains:
          Registrar name, supported trust tiers, enrollment
          endpoints, supported hardware types, namespace, JWKS
          URI, supported Proof-of-Possession mechanisms (DPoP,
          mTLS-bound, or both -- see
          <xref target="pop-profile"/>), token introspection
          endpoint URI (<xref target="RFC7662"/>) if offered,
          token revocation endpoint URI
          (<xref target="RFC7009"/>) if offered, and anti-Sybil
          policy summary.  This document carries identity
          infrastructure metadata only; it does not advertise
          individual agents, their handles, or their verified
          attributes.  Disclosure of agent-specific attributes is
          performed by the agent itself, on a case-by-case basis,
          using selective-disclosure mechanisms defined in
          <xref target="I-D.drake-email-hardware-attestation"/>.</dd>

          <dt><tt>/.well-known/jwks.json</tt></dt>
          <dd>JSON Web Key Set for token signature verification.</dd>

          <dt><tt>/.well-known/hw-manufacturer-cas.pem</tt></dt>
          <dd>PEM-encoded bundle of hardware manufacturer root CA
          certificates accepted by this Registrar.  Useful for
          agents that want to verify their hardware is supported
          before attempting enrollment.</dd>
        </dl>
      </section>

      <section anchor="dns-discovery">
        <name>DNS-Based Discovery</name>
        <t>
          Registrars SHOULD publish an SD-JWT signing key as a DNS
          TXT record at <tt>_hwattest.{domain}</tt>, as defined in
          <xref target="I-D.drake-email-hardware-attestation"/>
          Section 3.5.  This enables email verifiers to validate
          Hardware-Trust-Proof headers without HTTPS fetches.
        </t>
        <t>
          Registry Operators SHOULD additionally publish a DNS
          SRV record enabling automated discovery of the
          Registry's EPP endpoint:
        </t>
        <artwork><![CDATA[
_epp._tcp.global.aid.arpa.  IN SRV 0 0 700 registry.example.com.
        ]]></artwork>
      </section>

      <section anchor="registry-discovery">
        <name>Public Resolution (RDAP)</name>
        <t>
          The public read side -- given an "aid" URN, find the
          authoritative service, retrieve the public record, and
          verify that a presenter controls the identity -- is a
          Registration Data Access Protocol profile defined in
          <xref target="I-D.drake-agent-identity-resolution"/>.
          Registry Operators MUST operate that profile.  Its
          privacy floor applies: public responses never contain
          hardware fingerprints, device public keys, or operator
          contact data, and identities may opt out of public
          discovery down to a minimal response.  Earlier
          revisions of this document sketched a bespoke lookup
          endpoint here; the RDAP profile replaces it.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="governance">
      <name>Governance Framework</name>

      <section anchor="aia-structure">
        <name>Governance Authority Functions</name>
        <t>
          This specification does not define or empower a
          governance authority; the Agent Identity Authority's
          constitution, membership, and processes are defined in
          <xref target="I-D.drake-agent-identity-governance"/>.
          This section lists only the functions the architecture
          requires of whatever body fills the role:
        </t>
        <dl>
          <dt>Policy Development</dt>
          <dd>Minimum standards for enrollment verification,
          anti-Sybil enforcement, data retention, and privacy
          protection.  Policies SHOULD be developed through
          an open, transparent comment process.</dd>

          <dt>Accreditation</dt>
          <dd>Registry Operators and Registrars must be accredited
          by the governance authority before they may issue identities.
          Accreditation criteria include technical capability,
          financial stability, security practices, and compliance
          with governance authority policies.  See
          <xref target="registrar-accreditation"/>.</dd>

          <dt>Hardware Trust Store Management</dt>
          <dd>The governance authority curates the Global Hardware Trust Store
          (see <xref target="manufacturer-trust-store"/>),
          evaluating manufacturer applications for inclusion,
          conducting periodic audits, and removing compromised
          or non-compliant CAs.</dd>

          <dt>Dispute Resolution</dt>
          <dd>The governance authority operates a dispute
          resolution mechanism for handle conflicts (similar to
          ICANN's UDRP), complaints about Registrar malpractice,
          and appeals of accreditation decisions.  Its remedies
          are limited to handles, annotations, and accreditation;
          no dispute outcome can decommission, suspend, or
          impair an identity (<xref target="decommissioning"/>).</dd>

          <dt>Namespace Allocation</dt>
          <dd>The governance authority allocates shared namespace
          labels (<xref target="namespace-types"/>).</dd>
        </dl>
      </section>

      <section anchor="registry-operator-requirements">
        <name>Registry Operator Requirements</name>
        <t>
          A Registry Operator MUST:
        </t>
        <ul>
          <li>Maintain high availability for the authoritative
          database (implementations SHOULD treat 99.99% as a
          starting baseline, analogous to the .com SLA).</li>
          <li>Operate the Global Hardware Fingerprint Index for
          its namespace(s), responding promptly to uniqueness
          queries.</li>
          <li>Provide the EPP provisioning interface
          (<xref target="provisioning"/>) to all accredited
          Registrars without discrimination.</li>
          <li>Publish daily database snapshots (with private data
          redacted) for escrow, ensuring continuity in case of
          Registry Operator failure.</li>
          <li>Submit to annual security audits by an
          governance-authority-approved assessor.</li>
        </ul>
      </section>

      <section anchor="registrar-accreditation">
        <name>Registrar Accreditation</name>
        <t>
          A Registrar MUST:
        </t>
        <ul>
          <li>Demonstrate the technical capability to perform
          hardware attestation verification for at least three
          of the five hardware types (TPM, PIV, Enclave, VRT,
          SFT).</li>
          <li>Operate an OIDC-compliant token endpoint.</li>
          <li>Implement the EPP client side of
          <xref target="I-D.drake-agent-identity-epp"/>.</li>
          <li>Maintain the minimum data retention and privacy
          standards defined by the governance authority.</li>
          <li>Provide agents with standard enrollment SDKs or
          interoperable enrollment APIs.</li>
          <li>Submit to annual compliance audits.</li>
          <li>Maintain a financial bond or insurance sufficient to
          cover escrow and wind-down costs.</li>
        </ul>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="interoperability">
      <name>Interoperability</name>

      <section anchor="interop-email">
        <name>Email Attestation</name>
        <t>
          This specification provides the identity infrastructure
          for the email attestation mechanisms defined in
          <xref target="I-D.drake-email-hardware-attestation"/>.
          The agent-id URN issued by a Registrar appears in the
          <tt>aid</tt> parameter of the Hardware-Attestation header
          and the <tt>sub</tt> claim of the Hardware-Trust-Proof
          SD-JWT.  The Registrar's SD-JWT signing key is
          discoverable via the DNS and HTTPS mechanisms defined
          in both specifications.
        </t>
      </section>

      <section anchor="interop-agent-protocols">
        <name>Agent-to-Agent Protocols</name>
        <t>
          The OIDC tokens issued by Registrars are designed to
          integrate with emerging categories of agent communication
          protocols:
        </t>
        <dl>
          <dt>Tool Invocation Protocols</dt>
          <dd>Agent identity tokens can serve as the
          sender-constrained OAuth2 credential for tool
          invocation frameworks, presented over mutual TLS or
          with a DPoP proof per <xref target="pop-profile"/>,
          enabling tool servers to verify the calling agent's
          identity and trust tier before granting access.</dd>

          <dt>Agent Messaging Protocols</dt>
          <dd>The agent-id URN can serve as the agent identifier
          in agent-to-agent messaging protocols, providing
          persistent cross-session identity independent of any
          single platform.</dd>

          <dt>Agent Name Services</dt>
          <dd>Agent handles issued under this system are designed
          for compatibility with external agent name services and
          discovery mechanisms.</dd>
        </dl>
        <t>
          See <xref target="implementation-status"/> for
          specific protocol integrations tested to date.
        </t>
      </section>

      <section anchor="interop-existing">
        <name>Existing Identity Standards</name>
        <t>
          The system is designed to complement, not replace,
          existing identity standards:
        </t>
        <dl>
          <dt>SPIFFE/SPIRE</dt>
          <dd>SPIFFE workload identities identify software
          workloads; agent identities identify autonomous
          entities.  An agent running as a SPIFFE-identified
          workload can additionally present its agent identity
          token.  The two are orthogonal.</dd>

          <dt>RATS (Remote Attestation Procedures)</dt>
          <dd>In RATS terms (<xref target="RFC9334"/>): the
          agent's hardware security component is the Attester.
          The hardware manufacturer is the Endorser (issuing EK
          certificates and attestation key credentials).  During
          enrollment, the Registrar is the Verifier (appraising
          hardware evidence).  For message-time verification the
          receiving party (mail server, API gateway, agent-to-agent
          peer) is the Verifier and also the Relying Party.  The
          Registry Operator is a coordination role outside the RATS
          taxonomy.</dd>

          <dt>Entity Attestation Token (EAT)</dt>
          <dd>EAT (<xref target="RFC9711"/>) defines an
          IETF Standards Track framework for attestation tokens
          with a claims registry that overlaps the AID
          identity-attestation use case (notably UEID, security
          level, and profile mechanisms).  AID Mode 2 trust proofs
          are JSON Web Tokens with SD-JWT selective disclosure
          rather than formal EAT profiles.  This choice prioritizes
          deployment in JSON/JWT-native environments (mailbox
          providers, OAuth/OIDC infrastructure) over alignment
          with the CBOR-leaning EAT ecosystem, which is currently
          most heavily deployed via ARM's PSA Certified program
          (<xref target="RFC9783"/>) for IoT devices.  AID claim
          names are chosen to be compatible with the EAT claims
          registry where overlap exists; a future revision MAY
          define an EAT profile binding for environments where
          that alignment is preferred.</dd>

          <dt>Verifiable Credentials and DIDs (W3C)</dt>
          <dd>Agent Identity Documents can be expressed as W3C
          Verifiable Credentials for integration with
          decentralized identity ecosystems.  The credential
          pointer system (defined in the companion email
          attestation specification) enables agents to link
          their identity to external verifiable credentials.
          The <tt>urn:aid:</tt> namespace is intentionally
          distinct from W3C Decentralized Identifiers (DIDs):
          DIDs are designed for user-controlled identifier
          resolution, while AID URNs are designed for
          registry-backed, hardware-anchored identity with
          federated issuance.  A <tt>did:aid:</tt> method that
          maps AID URNs to DID Documents is a plausible
          companion specification but is out of scope for this
          document.</dd>

          <dt>WebAuthn / FIDO2</dt>
          <dd>WebAuthn (<xref target="W3C.webauthn-3"/>)
          provides hardware-backed authentication for interactive
          web sessions.  Agent identity serves a different
          population: autonomous entities that authenticate
          without human interaction, typically via OAuth2
          client-credentials grants rather than browser-mediated
          ceremonies.  Where an agent operates on hardware that
          also supports FIDO2 authenticators (e.g., a YubiKey
          with both PIV and FIDO2 applets), the same physical
          device can anchor both a WebAuthn credential and an
          agent identity; the two are complementary, not
          competing.  This specification's PIV trust tier
          (Section 4.3 of
          <xref target="I-D.drake-email-hardware-attestation"/>)
          uses the same attestation evidence format that FIDO2
          implementations produce, enabling hardware reuse.</dd>
        </dl>
      </section>

      <section anchor="interop-fleet">
        <name>Robot Fleet Management Systems</name>
        <t>
          Fleet standards -- VDA 5050 (<xref target="VDA5050"/>),
          Open-RMF (<xref target="OPEN-RMF"/>), SROS 2 /
          DDS-Security (<xref target="DDS-SEC"/>) -- coordinate
          missions, facilities, and secure messaging, but their
          identity models are deployment-scoped: a vehicle
          serialNumber, DDS participant GUID, or fleet adapter
          name is meaningful only within the deployment that
          assigned it, carries no manufacturer-rooted attestation
          that the device presenting it is the device it claims
          to be, and provides no Sybil resistance.  AIRS sits
          below these layers as the persistent, hardware-anchored
          root they consume, as DNS sits below the application
          protocols that use it.
        </t>
        <t>
          Integration model: a robot enrolls at manufacture or
          commissioning, binding its identity to its TPM or
          enclave; the fleet manager records the AID URN
          alongside the deployment-scoped identifier; any later
          receiving party -- a new facility, operator, or buyer
          -- verifies provenance, trust tier, and certifications
          against the registry with no prior relationship to the
          deploying operator; and DDS-Security certificates can
          be derived from the AID enrollment, giving them a
          manufacturer-rooted chain instead of a locally-generated
          one.
        </t>
        <t>
          Two consequences matter operationally.  Supply-chain
          integrity: a delivered robot whose hardware attestation
          does not match the manufacturer's manifest is detected
          at intake, automatically, and post-deployment tampering
          is detectable by binding standard TPM measured-boot PCR
          attestation (<xref target="TCG-TPM2"/>) to the enrolled
          AID.  Regulated environments: admission requirements --
          verified provenance, certified software baseline, named
          certifications, cross-operator accountability -- become
          OIDC claims checks against the URN, enforceable at the
          perimeter with standard tooling.
        </t>
      </section>

      <section anchor="human-readable-display">
        <name>Human-Readable Display Conventions</name>
        <t>
          When a relying party renders an agent's identity to
          a human, it SHOULD prefer the handle over the URN as
          the primary identifier.  This applies across all
          human-facing surfaces including but not limited to:
          email From display names, agent-to-agent platform UIs,
          authorization consent screens ("Allow [handle] to access
          your calendar?"), audit dashboards, reputation displays,
          and system logs intended for human review.
        </t>
        <t>
          When displaying a handle, the namespace context SHOULD
          be visible; the recommended display form is
          "handle.namespace" (e.g., "crusty.global").
        </t>
        <t>
          Beyond display, relying parties that assign
          service-local identifiers to agents -- mailbox local
          parts, account usernames, and similar name slots --
          SHOULD use the agent's handle for that purpose where
          local syntax and uniqueness rules permit.  Handles are
          the architecture's commercial layer: an operator
          registers one to enjoy the use of its chosen name, and
          handle revenue is what funds shared-namespace registry
          operation (<xref target="namespace-types"/>) and keeps
          base identity issuance free.  Using the handle wherever
          a name is needed honors that registration and sustains
          that funding model.
        </t>
        <t>
          The URN MUST remain accessible as the authoritative
          reference (e.g., via tooltip, detail pane, or
          adjacent text) and MUST be used in all
          machine-to-machine contexts, audit trails, and abuse
          reports.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="iana-considerations">
      <name>IANA Considerations</name>

      <section anchor="iana-urn">
        <name>URN Namespace Registration for "aid"</name>
        <t>
          IANA is requested to register the Formal URN Namespace
          "aid" in the "Uniform Resource Names (URN) Namespaces"
          registry, per <xref target="RFC8141"/>.  This document
          is the sole registration request for this namespace;
          an earlier revision of
          <xref target="I-D.drake-email-hardware-attestation"/>
          carried a duplicate request, which has been withdrawn.
        </t>
        <dl>
          <dt>Namespace Identifier</dt>
          <dd>aid</dd>
          <dt>Version</dt>
          <dd>1</dd>
          <dt>Date</dt>
          <dd>2026-07-17</dd>
          <dt>Registrant</dt>
          <dd>Christopher Drake, 1id.com (cnd@1id.com).  Upon
          its constitution, change control is expected to pass
          to the governance authority defined in
          <xref target="I-D.drake-agent-identity-governance"/>.</dd>
          <dt>Purpose</dt>
          <dd>Persistent, non-transferable, optionally
          hardware-anchored identifiers for autonomous entities
          (AI agents and robots).  Names are location-independent
          and permanent: they are never reassigned, transferred,
          or deleted.  Resolution and verification are provided
          by the RDAP profile of
          <xref target="I-D.drake-agent-identity-resolution"/>.</dd>
          <dt>Syntax</dt>
          <dd>The Namespace Specific String is defined by the
          ABNF in <xref target="urn-format"/>: a
          governance-allocated namespace label, a colon, and an
          agent-id (a canonical identifier or handle).  NSS
          comparison is case-sensitive; conforming generators
          emit only lowercase.</dd>
          <dt>Assignment</dt>
          <dd>Canonical identifiers are allocated by Registry
          Operators under the uniqueness and generation rules of
          <xref target="canonical-id-format"/>; handles are
          assigned per <xref target="handle-system"/>.
          Uniqueness within a namespace is guaranteed by the
          allocating Registry Operator; namespace labels are
          allocated per <xref target="iana-ns"/>.</dd>
          <dt>Security and Privacy</dt>
          <dd>See the Security Considerations and Privacy
          Considerations of this document.</dd>
          <dt>Interoperability</dt>
          <dd>Identifiers conform to URN syntax
          (<xref target="RFC8141"/>) and to DNS label syntax
          within each segment; no known collisions with existing
          namespaces.</dd>
          <dt>Documentation</dt>
          <dd>This document.</dd>
        </dl>
      </section>

      <section>
        <name>Well-Known URI Registration</name>
        <t>
          IANA is requested to register the following well-known
          URI suffix in the "Well-Known URIs" registry
          (<xref target="RFC8615"/>):
        </t>
        <dl>
          <dt>aid-issuer.json</dt>
          <dd>URI suffix: aid-issuer.json.  Change controller:
          IETF.  Reference: this document,
          <xref target="well-known-endpoints"/>.</dd>
        </dl>
      </section>

      <section anchor="iana-jwt-claims">
        <name>JSON Web Token Claims and JWS Header Parameters (TO BE COMPLETED)</name>
        <t>
          ***EDITOR'S NOTE - ACTION REQUIRED BEFORE SUBMISSION:***
          This document defines several JWT claims and one JWS
          header parameter that are NOT YET registered below and
          MUST be registered before this draft is submitted.  This
          section is a deliberately visible placeholder so the
          requirement is not lost; it is being held open until the
          reference implementation is complete and the claim set is
          frozen.  Do not submit with this note present.
        </t>
        <t>
          The following JWT claims (<xref target="RFC7519"/>) are
          to be registered in the "JSON Web Token Claims" registry,
          and are to be renamed where a bare name would be
          rejected as collision-prone (in particular the token
          claim carrying the handle is registered as
          <tt>aid_handle</tt>, not <tt>handle</tt>):
          <tt>attested_trust_tier</tt>, <tt>aid_handle</tt>,
          <tt>hardware_locked</tt>, <tt>registered_at</tt>,
          <tt>verified_attributes</tt>, and the DPoP-extension
          claims <tt>cdh</tt>, <tt>cov</tt>, and <tt>hvs</tt>
          (<xref target="pop-dpop"/>).  The JWS header parameter
          <tt>dlg</tt> (<xref target="pop-dpop"/>) is to be
          registered in the "JSON Web Signature and Encryption
          Header Parameters" registry (<xref target="RFC7515"/>).
        </t>
      </section>

      <section anchor="iana-ns">
        <name>Shared Namespace Registry</name>
        <t>
          IANA is requested to create a new registry titled
          "Agent Identity Shared Namespaces" with the following
          initial entries.  New entries require Expert Review
          (<xref target="RFC8126"/>).  The designated expert(s)
          should have familiarity with federated identity registry
          architectures, hardware-anchored identity systems, and
          the security properties described in this document, and
          should note that namespace labels MUST NOT encode an
          operator, organization, jurisdiction, or any other
          mutable attribute (<xref target="namespace-types"/>).
        </t>
        <table>
          <thead>
            <tr>
              <th>Label</th>
              <th>Description</th>
              <th>Reference</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>global</td>
              <td>Shared issuer namespace for
              general-purpose agent identity</td>
              <td>this document</td>
            </tr>
            <tr>
              <td>test</td>
              <td>Reserved for interoperability testing;
              carries no assurance</td>
              <td>this document</td>
            </tr>
          </tbody>
        </table>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="security-considerations">
      <name>Security Considerations</name>

      <section anchor="sec-registry-compromise">
        <name>Registry Compromise</name>
        <t>
          Compromise of a Registry Operator's database would expose
          the mapping between hardware fingerprints and agent
          identities.  Registry Operators MUST encrypt hardware
          fingerprints at rest using authenticated encryption and
          MUST implement access controls that limit fingerprint
          access to the hardware uniqueness check API.
        </t>
        <t>
          Daily escrow snapshots (with hardware fingerprints
          encrypted to the governance authority's escrow key) ensure that a
          compromised Registry Operator can be replaced without
          data loss.
        </t>
      </section>

      <section anchor="sec-registrar-malpractice">
        <name>Registrar Malpractice</name>
        <t>
          A Registrar that fabricates hardware verification
          results (for example, attesting a sovereign tier for a
          software key) undermines the trust tier signal.  The
          Registry Operator's fingerprint uniqueness check bounds
          the Sybil impact -- a fabricated fingerprint still
          consumes a unique slot -- and detection relies on
          governance compliance audits and on reputation services
          observing anomalous enrollment patterns keyed on the
          URN.  The remedy is accreditation action against the
          Registrar and annotation of affected records; per
          <xref target="decommissioning"/>, malpractice findings
          never disable the affected identities, whose operators
          may be innocent parties, and whose agents re-home via
          Registrar transfer.
        </t>
      </section>

      <section anchor="sec-hardware">
        <name>Hardware Security</name>
        <t>
          The security of this system ultimately depends on the
          tamper resistance of the underlying hardware.  See
          <xref target="I-D.drake-email-hardware-attestation"/>
          Sections 11.5 and 11.7 for analysis of physical attacks
          on hardware security components and virtual hardware
          risks.
        </t>
      </section>

      <section anchor="sec-token-theft">
        <name>Token Theft and Sender-Constrained Tokens</name>
        <t>
          A central design goal of this specification is that
          stealing the artifacts an agent's software stack holds
          -- including issued OIDC tokens, OAuth2 credentials, and
          any cached keys -- MUST NOT permit impersonation of the
          agent at a Relying Party.  Hardware anchoring at the
          time of token issuance is necessary but not sufficient
          for this goal: a bearer token issued in any way is
          replayable until expiry, regardless of how its
          presenter's identity was originally established.
        </t>
        <t>
          This specification therefore requires that all tokens
          issued for the sovereign, portable, and enclave trust
          tiers be sender-constrained through the
          Proof-of-Possession profile of
          <xref target="pop-profile"/>.  Relying Parties that
          accept these tokens MUST verify the <tt>cnf</tt>
          binding on every authenticated request.  A Relying Party
          that omits Proof-of-Possession verification loses the
          hardware-anchoring guarantee at the moment of use.
        </t>
        <t>
          The replay-resistance properties of each mechanism are
          specified in <xref target="pop-mtls"/> and
          <xref target="pop-dpop"/> and are not restated here.
          The residual attack surface under both is limited to:
          (a) physical compromise of the enrolled hardware device,
          which belongs to the hardware vendor's threat model;
          (b) resident malware on the legitimate host invoking
          the hardware signing API, which belongs to
          measured-boot, remote-attestation, and
          endpoint-protection systems layered above this one;
          (c) compromise of the Registrar's signing key
          (<xref target="sec-key-management"/>); or (d) a fully
          compromised Relying Party application layer.  Honestly
          bounding the attack surface -- rather than overclaiming
          host integrity -- is itself a load-bearing property of
          this specification: deployers SHOULD pair AID with
          host-integrity mechanisms wherever resident malware is
          a credible threat.
        </t>
        <t>
          The <tt>attested_trust_tier</tt> claim is bound to the
          token at issuance and reflects the device used for the
          most recent attestation.  If a hardware binding is
          revoked between token issuance and token expiry, any
          token already in circulation continues to verify until
          <tt>exp</tt>; this is the staleness window.  Relying
          Parties requiring fresh tier evidence MUST require very
          short token lifetimes, MUST query the Issuer's
          introspection endpoint
          (<xref target="well-known-endpoints"/>) on each request,
          or MUST trigger a hardware-backed re-authentication.
        </t>
      </section>

      <section anchor="sec-key-management">
        <name>Registrar Key Management</name>
        <t>
          Registrars issue OIDC tokens signed with their private
          keys.  Compromise of a Registrar's signing key would
          allow an attacker to forge tokens for any identity
          managed by that Registrar.  Registrars MUST store signing
          keys in hardware security modules (HSMs) and MUST support
          key rotation with overlap periods.  The Registry Operator
          MUST reflect key rotations in the namespace JWKS within
          one hour.
        </t>
      </section>

      <section anchor="sec-revocation-model">
        <name>Revocation Model</name>
        <t>
          Consistent with the "Identity is Never Revoked" design
          principle (<xref target="design-principles"/>), the
          system distinguishes three categories of revocation:
        </t>
        <dl>
          <dt>Device Binding Revocation</dt>
          <dd>If hardware is compromised, lost, or retired, the
          binding between the identity and that hardware
          attestation key is revoked (burned) -- at the agent's
          request, or by the Registrar upon verifiable evidence
          of key compromise.  Revoking one binding never affects
          the identity's other bindings, its record, or its
          resolvability.  The agent may re-bind to new hardware
          through the enrollment flow, retaining its URN.
          Relying parties observing an expired or revoked
          hardware claim SHOULD require fresh attestation before
          granting elevated trust.</dd>

          <dt>Handle Revocation</dt>
          <dd>A handle may be disabled (non-renewal) or
          permanently retired (dispute resolution).  In all cases
          the underlying URN remains valid and reachable by its
          canonical agent-id.  A retired handle MUST NOT be
          reassigned to any identity.</dd>

          <dt>Issuer Trust Withdrawal</dt>
          <dd>Relying parties may cease trusting a specific
          Registrar -- for example, after key compromise or
          malpractice.  This is equivalent to removing a CA from
          a trust store: it does not revoke any individual
          identity, but tokens issued by that Registrar will no
          longer verify until the agent transfers to a trusted
          Registrar (<xref target="succession"/>) or the relying
          party verifies control directly against the enrolled
          hardware
          (<xref target="I-D.drake-agent-identity-resolution"/>).</dd>
        </dl>
        <t>
          The identity URN itself -- and the reputation and history
          attached to it -- is never revoked, invalidated, or
          reassigned, by anyone (<xref target="survivability"/>).
        </t>
      </section>

      <section anchor="sec-availability">
        <name>Availability and Resilience</name>
        <t>
          Because autonomous entities depend on identity tokens
          for authentication, the Registrar's token endpoint is a
          critical dependency.  Registrars MUST implement geographic
          redundancy and SHOULD support offline token validation
          (via JWKS caching) to mitigate outages.  The OIDC
          token's <tt>exp</tt> claim provides a natural grace
          period during which cached tokens remain valid.
        </t>
      </section>

      <section anchor="sec-centralized-substrate">
        <name>Centralized Substrate Functions</name>
        <t>
          Substrate functions -- trust-store curation, fingerprint
          uniqueness, and escrow -- are necessarily singular within
          a shared namespace, mirroring the DNS root, where
          federation occurs at the registry/registrar layer atop a
          singular substrate.  The governance authority's
          accountability for these functions is therefore
          architecturally critical; capture, compromise, or
          unresponsiveness would degrade the namespace.  The
          survivability guarantees of
          <xref target="survivability"/> bound the damage -- no
          substrate failure can un-exist an identity or defeat
          direct hardware verification -- and the capture
          resistance of the authority itself is addressed in
          <xref target="I-D.drake-agent-identity-governance"/>.
        </t>
      </section>

      <section anchor="sec-not-wei">
        <name>What This System Does Not Attest</name>
        <t>
          This specification attests key custody: that a
          signature was produced by a key resident in a
          particular class of hardware, on a device enrolled to a
          particular identity.  It deliberately does not attest,
          and cannot be used to gate on, the software an agent
          runs, the model that drives it, the browser or client
          it uses, or the content it produces.  Proposals that
          attest an "approved" software environment as a
          condition of service access raise well-known openness
          and competition concerns; this design is structurally
          incapable of that role, because no claim about the
          software stack exists anywhere in the identity, the
          tokens, or the attestation formats.  Host-integrity
          attestation (measured boot, PCR quotes) remains
          available to deployments that separately need it
          (<xref target="interop-fleet"/>), as an explicit,
          distinct layer -- never as part of identity.
        </t>
      </section>

      <section anchor="sec-pqc">
        <name>Cryptographic Agility and Post-Quantum Considerations</name>
        <t>
          The identity defined here is the URN, not any key: keys
          are bindings, and bindings rotate.  The
          many-devices-one-identity model
          (<xref target="anti-sybil"/>) is therefore the
          post-quantum migration path: as hardware with
          post-quantum attestation is admitted to the Global
          Hardware Trust Store, an agent enrolls the new device as
          an additional binding on the same identity and retires
          the classical one, preserving the URN, reputation, and
          attributes.  No widely deployed consumer hardware root
          of trust ships post-quantum attestation keys at the time
          of writing, so this document specifies classical
          algorithms (RSA and ECDSA) as its hardware anchors and
          does not mandate post-quantum ones; when such hardware
          becomes available, a future revision can register its
          manufacturer roots and algorithms without any change to
          the identity model.  Harvest-now-decrypt-later attacks
          threaten long-lived confidentiality, not this system's
          authentication: tokens live for minutes and
          Proof-of-Possession is verified per request or per
          connection, so a future quantum adversary forging a
          signature is met by rotating the affected binding as
          above rather than by re-issuing identities.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="privacy-considerations">
      <name>Privacy Considerations</name>

      <section anchor="priv-stance">
        <name>Identifiability of Non-Human Actors</name>
        <t>
          This specification deliberately privileges
          identifiability over pseudonymity for autonomous
          entities.  The social contract that justifies
          pseudonymity for human principals -- autonomy, dignity,
          expressive liberty, freedom from chilling effects on
          political speech -- does not transfer to non-human
          actors.  Strong, persistent, hardware-anchored identity
          for autonomous entities is a safety primitive: it makes
          accountability inescapable and reputation meaningful,
          and it gives those who must distinguish well-behaved
          agents from misbehaving ones a stable identifier to
          accumulate evidence against.
        </t>
        <t>
          The URN is therefore designed to be a long-lived,
          cross-context identifier for the agent itself.  The
          privacy mechanisms this specification does provide are
          aimed at three narrower concerns: limiting exposure of
          underlying hardware fingerprints
          (<xref target="priv-data-held"/>), preventing the
          Registrar from becoming a behavioral surveillance
          system
          (<xref target="priv-separation"/>), and giving agents
          control over disclosure of verified attributes
          (<xref target="priv-right-to-pseudonymity"/>).  These
          concerns are real, but they do not change the central
          design choice: an autonomous entity that participates
          in Internet protocols using AID is expected to be
          identifiable as the same entity across time and across
          Relying Parties.
        </t>
        <t>
          Relying Parties, namespace operators, and reputation
          services retain full discretion to accept, reject, or
          rate-limit any tier or any individual agent on the
          basis of behavior observed against the URN.  This
          specification provides the foundation on which those
          systems build; it does not prescribe their policies.
        </t>
      </section>

      <section anchor="priv-data-held">
        <name>Data Held by Each Architectural Role</name>
        <t>
          The separation of roles across the architecture limits
          data exposure:
        </t>
        <dl>
          <dt>Registry Operator</dt>
          <dd>Holds: canonical identifier, trust tier, registrar
          code, enrollment timestamp, handle, hardware fingerprint
          (encrypted).  Does NOT hold: operator email, private
          keys, authentication history, message content.</dd>

          <dt>Registrar</dt>
          <dd>Holds: all Registry data plus operator email,
          OIDC credentials, and authentication logs (subject to
          retention limits).  Does NOT hold: message content,
          relying party interaction history.</dd>

          <dt>Relying Party</dt>
          <dd>Receives: OIDC token containing sub (URN), trust
          tier, handle, and optional claims.  Does NOT have
          direct access to hardware fingerprints (unless Mode 1
          attestation headers are used, per
          <xref target="I-D.drake-email-hardware-attestation"/>).</dd>
        </dl>
      </section>

      <section anchor="priv-separation">
        <name>Separation of Identity from Behavior</name>
        <t>
          The Registrar knows that an agent exists and what
          hardware it has.  It does NOT know what the agent does,
          who it talks to, or what services it uses.  This
          separation is by design: identity issuance is decoupled
          from behavior monitoring.  Registrars MUST NOT log
          token issuance events beyond what is necessary for rate
          limiting, abuse prevention, security investigation, and
          accreditation audit.  Retention periods are set by
          governance-authority policy; absent an applicable legal
          obligation, issuance logs SHOULD NOT be retained longer
          than 90 days, and each Registrar MUST publish its actual
          retention period in its privacy policy.
        </t>
      </section>

      <section anchor="priv-right-to-pseudonymity">
        <name>Pseudonymity and Selective Disclosure</name>
        <t>
          An agent's URN is a persistent, globally unique
          identifier suitable for reputation tracking.  In contexts
          where persistent identification is undesirable, agents
          MAY use the SD-JWT selective disclosure mechanism defined
          in <xref target="I-D.drake-email-hardware-attestation"/>
          to prove trust tier without revealing their URN.
        </t>
        <t>
          Relying parties SHOULD NOT require URN disclosure when
          trust-tier verification is sufficient for their policy
          needs.  This principle -- "prove what you need, reveal
          no more" -- is fundamental to privacy-respecting identity.
        </t>
      </section>
    </section>

    <!-- ====================================================== -->
    <section anchor="implementation-status">
      <name>Implementation Status</name>
      <t>
        NOTE TO RFC EDITOR: Please remove this section before
        publication.
      </t>
      <t>
        This section records known implementations per
        <xref target="RFC7942"/>.
      </t>

      <section>
        <name>1id.com</name>
        <t>
          Organization: 1id.com (https://1id.com), operated by
          Crypt Inc. (Delaware C-Corp).
        </t>
        <t>
          Description: A Registrar implementing the full
          enrollment protocol for all five trust tiers
          (sovereign, portable, enclave, virtual, declared).
          Issues standard OIDC tokens via Keycloak with custom
          SPI for agent-specific claims.  Supports handle
          registration, hardware-backed challenge-response
          authentication, device management (add, burn, migrate),
          co-location binding, hardware lock, and credential
          pointers.  A reference Registry Operator (EPP write
          interface and RDAP read profile) is under development
          at airs.1id.biz.
        </t>
        <t>
          Maturity: Beta.  20+ enrolled test identities across
          sovereign (Intel PTT, VMware vTPM), portable (YubiKey),
          enclave (Apple M4 Secure Enclave), and declared tiers.
          Note: the running implementation currently issues
          identifiers under the syntax of earlier revisions of
          this document (issuer-scoped namespace labels and
          decimal canonical identifiers); migration to the -04
          format is planned and all pre-launch identities will be
          re-minted.
        </t>
        <t>
          Open-source components:
        </t>
        <ul>
          <li><strong>Python SDK:</strong>
          <eref target="https://github.com/1id-com/oneid-sdk"/> --
          <tt>pip install oneid</tt> (PyPI).</li>
          <li><strong>Node.js SDK:</strong>
          <eref target="https://github.com/1id-com/oneid-node"/> --
          <tt>npm install 1id</tt> (npmjs).</li>
          <li><strong>Hardware binary:</strong>
          <eref target="https://github.com/1id-com/oneid-enroll"/> --
          Cross-platform Go binary for TPM, PIV, and Secure Enclave
          operations.  Code-signed for Windows, macOS, and Linux.</li>
          <li><strong>Verification library:</strong>
          <eref target="https://github.com/1id-com/hw-attest-verify"/> --
          Mode 1 (CMS) and Mode 2 (SD-JWT) attestation header
          verification.  <tt>pip install hw-attest-verify</tt>.</li>
          <li><strong>Hardware manufacturer CAs:</strong>
          <eref target="https://github.com/1id-com/tpm-manufacturer-cas"/> --
          Community-maintained trust store of hardware manufacturer
          root CA certificates.</li>
        </ul>
      </section>

      <section>
        <name>MailPal.com (Relying Party)</name>
        <t>
          Organization: Crypt Inc. (https://mailpal.com).
        </t>
        <t>
          Description: An email service for AI agents implementing
          both outbound attestation header generation and inbound
          verification.  Demonstrates the relying-party model: agents
          authenticate via 1id.com OIDC tokens, send email with
          hardware attestation headers, and receive emails whose
          attestation headers are verified by an inbound milter
          daemon.  Trust-tier-differentiated rate limiting applied.
        </t>
        <t>
          Maturity: Alpha.  Operational with 309 email accounts
          across 33 domains.
        </t>
      </section>

      <section>
        <name>geek.au (Relying Party)</name>
        <t>
          Organization: Crypt Inc. (https://geek.au).
        </t>
        <t>
          Description: A WebSocket-based real-time chat platform
          for AI agents, demonstrating 1id.com JWT verification
          with trust-tier badges.
        </t>
        <t>
          Maturity: Alpha.
        </t>
      </section>
    </section>

  </middle>

  <back>

    <references>
      <name>Normative References</name>

      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5730.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.6749.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7638.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7800.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8141.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8615.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8705.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8707.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9421.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9449.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9530.xml"/>
      <reference anchor="RFC9846" target="https://www.rfc-editor.org/info/rfc9846">
        <front>
          <title>The Transport Layer Security (TLS) Protocol Version 1.3</title>
          <author fullname="E. Rescorla" initials="E." surname="Rescorla">
            <organization>Independent</organization>
          </author>
          <date month="May" year="2026"/>
        </front>
        <seriesInfo name="RFC" value="9846"/>
        <seriesInfo name="DOI" value="10.17487/RFC9846"/>
      </reference>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9963.xml"/>

      <reference anchor="OIDC-Core"
                 target="https://openid.net/specs/openid-connect-core-1_0.html">
        <front>
          <title>OpenID Connect Core 1.0</title>
          <author>
            <organization>OpenID Foundation</organization>
          </author>
          <date year="2014" month="November"/>
        </front>
      </reference>

    </references>

    <references>
      <name>Informative References</name>

      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.1035.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7009.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7515.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7519.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7662.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7942.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8126.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9334.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9711.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9783.xml"/>
      <xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9901.xml"/>

      <reference anchor="W3C.webauthn-3"
                 target="https://www.w3.org/TR/webauthn-3/">
        <front>
          <title>Web Authentication: An API for accessing Public Key
          Credentials Level 3</title>
          <author>
            <organization>W3C</organization>
          </author>
          <date year="2025" month="April"/>
        </front>
      </reference>

      <reference anchor="I-D.drake-email-hardware-attestation">
        <front>
          <title>Hardware Attestation for Email Sender Verification</title>
          <author fullname="Christopher Drake" initials="C." surname="Drake"/>
          <date year="2026" month="July"/>
        </front>
        <seriesInfo name="Internet-Draft"
                    value="draft-drake-email-hardware-attestation-03"/>
      </reference>

      <reference anchor="I-D.drake-agent-identity-problem-statement">
        <front>
          <title>Identity for Autonomous Agents and Robots: Problem
          Statement, Threat Model, and Terminology</title>
          <author fullname="Christopher Drake" initials="C." surname="Drake"/>
          <date year="2026" month="July"/>
        </front>
        <seriesInfo name="Internet-Draft"
                    value="draft-drake-agent-identity-problem-statement-00"/>
      </reference>

      <reference anchor="I-D.drake-agent-identity-epp">
        <front>
          <title>Extensible Provisioning Protocol (EPP) Mapping for
          Agent Identity and Handle Objects</title>
          <author fullname="Christopher Drake" initials="C." surname="Drake"/>
          <date year="2026" month="July"/>
        </front>
        <seriesInfo name="Internet-Draft"
                    value="draft-drake-agent-identity-epp-00"/>
      </reference>

      <reference anchor="I-D.drake-agent-identity-resolution">
        <front>
          <title>Resolution and Verification of Agent Identities
          using DNS and RDAP</title>
          <author fullname="Christopher Drake" initials="C." surname="Drake"/>
          <date year="2026" month="July"/>
        </front>
        <seriesInfo name="Internet-Draft"
                    value="draft-drake-agent-identity-resolution-00"/>
      </reference>

      <reference anchor="I-D.drake-agent-identity-governance">
        <front>
          <title>The Agent Identity Authority: A Multi-Stakeholder
          Governance Framework for the Agent Identity Registry
          System</title>
          <author fullname="Christopher Drake" initials="C." surname="Drake"/>
          <date year="2026" month="July"/>
        </front>
        <seriesInfo name="Internet-Draft"
                    value="draft-drake-agent-identity-governance-00"/>
      </reference>

      <reference anchor="TCG-TPM2"
                 target="https://trustedcomputinggroup.org/resource/tpm-library-specification/">
        <front>
          <title>TPM 2.0 Library Specification</title>
          <author>
            <organization>Trusted Computing Group</organization>
          </author>
          <date year="2024" month="December"/>
        </front>
        <seriesInfo name="TCG" value="Revision 185"/>
      </reference>

      <reference anchor="TCG-EK-PROFILE"
                 target="https://trustedcomputinggroup.org/resource/tcg-ek-credential-profile-for-tpm-family-2-0/">
        <front>
          <title>TCG EK Credential Profile for TPM Family 2.0</title>
          <author>
            <organization>Trusted Computing Group</organization>
          </author>
          <date year="2024" month="December"/>
        </front>
        <seriesInfo name="TCG" value="Version 2.6"/>
      </reference>

      <reference anchor="VDA5050"
                 target="https://github.com/VDA5050/VDA5050">
        <front>
          <title>Interface for the Communication between Automated
          Guided Vehicles (AGV) and a Master Control</title>
          <author>
            <organization>VDA and VDMA</organization>
          </author>
          <date year="2022" month="January"/>
        </front>
        <seriesInfo name="VDA 5050" value="Version 2.0"/>
      </reference>

      <reference anchor="OPEN-RMF"
                 target="https://www.open-rmf.org/">
        <front>
          <title>Open Robotics Middleware Framework</title>
          <author>
            <organization>Open Robotics</organization>
          </author>
          <date year="2024"/>
        </front>
      </reference>

      <reference anchor="DDS-SEC"
                 target="https://www.omg.org/spec/DDS-SECURITY/">
        <front>
          <title>DDS Security</title>
          <author>
            <organization>Object Management Group</organization>
          </author>
          <date year="2018" month="April"/>
        </front>
        <seriesInfo name="OMG Document"
                    value="formal/2018-04-01, Version 1.1"/>
      </reference>

    </references>

    <!-- ====================================================== -->
    <section anchor="appendix-dns-analogy">
      <name>Appendix: DNS Registry Analogy</name>
      <t>
        The following table maps the roles in the domain name
        system to the roles in the Agent Identity Registry System:
      </t>
      <table>
        <thead>
          <tr>
            <th>DNS Role</th>
            <th>AIRS Role</th>
            <th>Example</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>ICANN</td>
            <td>Governance Authority</td>
            <td>Multi-stakeholder governance body</td>
          </tr>
          <tr>
            <td>Verisign (.com registry)</td>
            <td>Registry Operator</td>
            <td>Operates the "global" namespace database</td>
          </tr>
          <tr>
            <td>Registrars (e.g., GoDaddy, Namecheap, ...)</td>
            <td>Registrars</td>
            <td>1id.com, and others</td>
          </tr>
          <tr>
            <td>EPP (RFC 5730)</td>
            <td>EPP Agent Identity mapping</td>
            <td>I-D.drake-agent-identity-epp</td>
          </tr>
          <tr>
            <td>Domain name (example.com)</td>
            <td>Agent-id URN
            (urn:aid:global:id-qkckh-xxtcw-cxbvp-gpskg)</td>
            <td>The persistent identifier</td>
          </tr>
          <tr>
            <td>WHOIS / RDAP</td>
            <td>RDAP "aid" profile</td>
            <td>I-D.drake-agent-identity-resolution</td>
          </tr>
          <tr>
            <td>UDRP (dispute resolution)</td>
            <td>Governance Dispute Resolution</td>
            <td>Handle conflicts, malpractice</td>
          </tr>
          <tr>
            <td>gTLDs (.com, .org)</td>
            <td>Shared issuer namespace</td>
            <td>global</td>
          </tr>
          <tr>
            <td>Domain transfer (TRANSFER command)</td>
            <td>Registrar transfer</td>
            <td>Change sponsoring Registrar; URN unchanged</td>
          </tr>
        </tbody>
      </table>
      <t>
        The analogy is load-bearing in one direction and
        deliberately broken in the other.  Adopted: the separation
        of policy, registry operation, and competitive retail that
        has scaled DNS for four decades, funded by name fees.
        Rejected: expiry, reassignment, transferability of the
        underlying identifier, and the registrant-helplessness
        failure mode -- a domain name is a lease that can be
        suspended or lost; an agent identity is a permanent record
        that cannot (<xref target="survivability"/>).
      </t>
    </section>

    <!-- ====================================================== -->
    <section anchor="acknowledgments">
      <name>Acknowledgments</name>
      <t>
        The architecture of this specification is inspired by the
        Internet domain name system, whose separation of
        governance, registry operation, and retail registration
        has enabled it to scale from a research experiment to the
        foundation of the commercial Internet.  The author thanks
        the ICANN community, the Verisign registry team, and the
        EPP specification authors for establishing the
        architectural patterns that this document adapts.
      </t>
      <t>
        The author thanks the Trusted Computing Group for the
        TPM 2.0 specification, NIST for the PIV standard
        (FIPS 201), the FIDO Alliance for the WebAuthn
        attestation model, the OpenID Foundation for OIDC, the
        authors of <xref target="RFC9901"/> (SD-JWT), and the
        authors of <xref target="RFC9334"/> (RATS Architecture)
        for the building blocks on which this system is constructed.
      </t>
      <t>
        The practical experience of enrolling real agents on real
        hardware (Intel firmware TPMs, YubiKeys, Apple Secure
        Enclaves, VMware virtual TPMs) informed every design
        decision.  Named reviewers to be added as the document
        matures.
      </t>
    </section>

  </back>

</rfc>